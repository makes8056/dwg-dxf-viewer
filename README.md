# DWG・DXFビューア

CADの図面（DXF／DWG）を、iPadやパソコンのブラウザで開いて見るためのアプリです。
管工事の現場・出先で、図面を確認して**必要な部分だけを印刷する**ことを目的にしています。

## できること（作っている途中です）

| | 内容 | 状態 |
| :-- | :-- | :-- |
| 1 | 図面（DXF）を開いて見る。スワイプで移動、ピンチで拡大縮小 | できます |
| 2 | **範囲を枠で囲んで、その部分だけ印刷する**（このアプリの一番の目的） | できます |
| 3 | **DWGをそのまま開く** | できます |
| 4 | 開いた図面を覚えておく（10件まで） | できます |
| 5 | ホーム画面に追加してオフラインで使う | できます |
| 6 | 2点をタップして寸法を測る。図面に文字を書き足す | これから |

### DWGについて（はじめて開くとき）

DWGを読むための部品は**約10MB**あります。
アプリを軽く保つため、**DWGを開こうとしたときに、はじめて読み込みます。**

そのため、**はじめてDWGを開くときだけ電波が必要です。**
一度読み込めば端末にしまわれるので、次からは電波の無い現場でもDWGを開けます
（アプリを更新しても、この部品は読み直しになりません）。

現場へ出る前に、電波のあるところで一度DWGを開いておくことをおすすめします。

## 図面ファイルは外に出ません

**このアプリはサーバーを使いません。**
図面を読むのも、描くのも、印刷用に加工するのも、すべてお使いの端末（iPad）の中だけで行います。
図面ファイルがインターネットのどこかへ送られることはありません。

## アプリを開く

**https://makes8056.github.io/dwg-dxf-viewer/**

## 使い方（iPad）

1. Safariで上のページを開く
2. 「図面を開く」を押して、ファイルアプリやiCloudから図面（DXFまたはDWG）を選ぶ
3. 指1本でなぞると移動、指2本でつまむと拡大縮小
4. 「印刷する範囲」を押して、印刷したいところを指で囲む
   （囲んだあと、丸をつまんで大きさを直せます。指2本で図面の拡大縮小もできます）

**電波の無い現場でも使いたい場合は、必ずSafariで開いてください。**
Safariの共有ボタンから「ホーム画面に追加」を選ぶと、アイコンから起動できるようになり、
電波が無くても開けます。

iPadのChromeでは、この「ホーム画面に追加」が使えない決まりになっています
（iPadのChromeは、中身がSafariと同じ仕組みで動いているためです）。
Chromeでも図面を見ること自体はできますが、そのつど電波が必要です。

## 作り方

- 素のJavaScript（ES2022）＋ESモジュール。**ビルドは使いません。**
  npm・React・Vite・TypeScriptは使いません。ファイルをそのまま置けば動きます。
- 描画は HTML5 Canvas の2D。
- 外部の部品は `vendor/` にファイルとして同梱します（外部サーバーから読み込みません）。
  同梱している部品は `vendor/libredwg/README.md` を見てください。
- DWGは、**DXFに変換してからDXFと同じ道で読みます**（読み解く係を2つ作らないため）。

詳しくは `開発ルール.md` を見てください。

## 使用許諾（ライセンス）

このアプリは **GPL-3.0** で公開しています（`LICENSE` を見てください）。

DWG形式を読むために `libredwg-web`（LibreDWGをWebAssembly化したもの）を同梱しており、
これがGPL-3.0であるためです。

公開されるのは**アプリのプログラムだけ**です。お客様の図面は公開されません。

## 置き場所

```text
H:\マイドライブ\AIアプリ制作\05_DWG-DXFビューア
```

## 作業を始めるAIへ

次の順番で読んでください。

1. `H:\マイドライブ\AIアプリ制作\00_共通ルール\まず読む.md`
2. `H:\マイドライブ\AIアプリ制作\00_共通ルール\CodexとClaude Codeへのお願い.md`
3. このフォルダーの `作業コンテキスト.md`
4. このフォルダーの `開発ルール.md`

## テストの動かし方

```bash
node --test
```

このフォルダーの一番上で実行してください（`node --test tests/` では動きません）。
npmのインストールは要りません。
