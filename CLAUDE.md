# CLAUDE.md（DWG・DXFビューア）

## 読む順番

1. `H:\マイドライブ\AIアプリ制作\00_共通ルール\まず読む.md`
2. `H:\マイドライブ\AIアプリ制作\00_共通ルール\CodexとClaude Codeへのお願い.md`
3. このフォルダーの `作業コンテキスト.md`
4. このフォルダーの `開発ルール.md`

## このアプリでの約束

- 説明はすべて日本語。専門用語は使うなら意味を添える。
- アプリ本体を変えたら版番号を上げる（`src/version.js` と `service-worker.js` の両方）。
- ワーカー（サブエージェント）には commit / push をさせない。司令塔がまとめる。
- ユーザーが置いた図面ファイル（`.dwg` `.dxf` `.pdf` `.bak`）はコミットしない。
- 「テストが通った」は完了ではない。実物を見て、公開して、はじめて完了。

## 参考

`H:\マイドライブ\AIアプリ制作\04寸法取りアプリ制作` が同じ作り方の先行例です。
特に `service-worker.js`（PWAの事故対策）と `開発ルール.md` は流用できます。
