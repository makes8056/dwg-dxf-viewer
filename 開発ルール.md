# 開発ルール（DWG・DXFビューア）

04寸法取りアプリで実際に痛い目を見て決まったルールのうち、
**このアプリでも最初から守るもの**をまとめました。
アプリ固有のルール（図面の描き方など）は、決まり次第この下に章を足していきます。

---

## 1章 技術構成

1.1 素のJavaScript（ES2022）とESモジュールで作る。npm・ビルド・フレームワークは使わない。
　　node は開発用の道具（テスト・生成スクリプト）にだけ使う。

1.2 理由：現場で長く使うアプリで、数年後にビルドが通らなくなる事故を避けるため。
　　ブラウザにそのまま置けば動く状態を保つ。

1.3 **例外を作りたくなったら、先にユーザーへ相談する。**
　　特にDWG対応は外部ライブラリが要る可能性が高い。黙って入れない。

---

## 2章 ファイルの分け方

2.1 画面の入口は `index.html`。そこから読み込むスクリプトは入口1つ（`src/ui/app.js` など）だけにし、
　　あとは `import` でつなぐ。

2.2 **1ファイル1役割。** 最初から小さく分ける。あとで分けるのは高くつく。

2.3 他の機能の中身を直接さわらない。やり取りは決められた関数（窓口）越しにする。

2.4 描画のモジュールは、データを自分で取りに行かない。
　　**呼び出す側が計算して渡す**（setter で渡す）形にする。
　　04ではこれを守ったおかげで、描画だけを差し替える改修が安全にできた。

---

## 3章 版番号

3.1 アプリ本体（画面・機能・計算・データ・CSS・文言）を変えたら、**必ず版番号を1つ上げる。**

3.2 版番号は2か所にある。**両方を手で合わせる。**
　　- `src/version.js` の `APP_VERSION`（画面に出る）
　　- `service-worker.js` の `CACHE_VERSION`（キャッシュを切り替える）

3.3 `CACHE_VERSION` を上げ忘れると、中身を直してもブラウザが「更新なし」と判断し、
　　**古いままの画面がユーザーに出続ける。** これは実際に起きた。

3.4 記録（`作業コンテキスト.md`）とコミットメッセージにも新しい版番号を残す。

3.5 記録だけの修正や、GitHubの設定だけの作業では版番号を上げない。

---

## 4章 git の運用

4.1 このフォルダー自体をgitリポジトリにする。

4.2 **ワーカー（サブエージェント）には git add / commit / push をさせない。**
　　司令塔がまとめてコミットする。
　　理由：複数のワーカーが同時に作業すると、途中の壊れた状態が混ざって記録される。

4.3 コミットメッセージは日本語で、「何をしたか」がわかるように書く。

4.4 ユーザーが置いた資料ファイル（図面、PDF、`.dwg`、`.bak` など）は**コミットしない。**
　　`.gitignore` に入れる。04では毎回これに気をつけていた。

4.5 push すると公開先へ自動で反映される想定。**push は「配布」と同じ意味を持つ。**
　　壊れたものを push しない。

---

## 5章 PWA（ホーム画面から使う）

5.1 オフラインで動くよう、アプリの本体ファイルは Service Worker で事前キャッシュする。

5.2 **黙って更新しない。** 新しい版を見つけたら画面に「更新があります」と出し、
　　ユーザーが押したときだけ切り替える。作業中のデータを飛ばさないため。

5.3 **【04で起きた本番事故】リダイレクトされたレスポンスをキャッシュに入れない・返さない。**

　　Cloudflare Pages は `/index.html` を `/` へリダイレクトする。
　　そのままキャッシュすると `redirected: true` のレスポンスが保存され、
　　iPadのホーム画面から起動したときだけ
　　「Response served by service worker has redirections」
　　というエラーで**アプリが一切開かなくなる。**

　　対策は2つセットで必要：
　　- `manifest.json` の `start_url` を `"./"` にする
　　- 保存するときと返すときの両方で、body と headers から Response を作り直す
　　　（返す側もやらないと、すでに壊れた端末が自力で直らない）

　　04の `service-worker.js` に実装と解説のコメントがあるので、そのまま持ってくる。

5.4 実データ（IndexedDB）には Service Worker を触らせない。
　　キャッシュとデータ保存は別物だと構造で保証しておく。

---

## 6章 テストとベースライン

6.1 **弾（作業のひとまとまり）を始める前に、テストが今いくつ通っているかを数えておく。**

6.2 理由：あとで失敗が出たとき、「もともと壊れていた」のか「今回壊した」のかを
　　区別できるのは、この数字だけ。
　　04では、ワーカーが「これは元からある無関係な不具合です」と報告してきたのを、
　　この数字があったおかげで**今回の作業が原因だと証明できた。**

6.3 壊れないことを守るテストは、**わざと壊してみて、ちゃんと失敗することを確かめる。**
　　通っているだけのテストは、何も守っていないことがある。

6.4 自動生成するデータファイルは、**生成しなおしても同じものができること**をテストで守る。
　　04では、生成スクリプトを放置したまま出力を手で直していたため、
　　作り直した瞬間にデータが静かに欠けた。

---

## 7章 ワーカー（サブエージェント）の使い方

7.1 司令塔は、Agentツールで自分でワーカーを起動する。
　　（指示文をユーザーに渡して代わりに動かしてもらう、ではない）
　　種類：general-purpose ／ モデル sonnet ／ バックグラウンド

7.2 **作業を頼む前に、決めごとをこの `開発ルール.md` に章として書く。**
　　口頭で頼んだことは守られない。文書にした約束だけが守られる。

7.3 **ワーカーの報告をそのまま信じない。**
　　「できました」「テストは通りました」は、確認の代わりにはならない。
　　司令塔は**実物を自分の目で見る。**
　　04では、テストが全部通っているのにボタンが重なっていた、
　　機能を作ったのにどこからも呼ばれていなかった、が3件見つかった。

7.4 見た目の確認のしかた（04で確立した方法）
　　- canvasの絵：`canvas.toDataURL()` → ローカルの受け口へPOST → PNGを画像として見る
　　- 画面の配置：`getBoundingClientRect()` で実際の座標を測る
　　- 「動くはず」ではなく「動いている」を数字か画像で確かめる

7.5 ワーカーは利用制限で突然止まることがある。
　　止まったら司令塔が引き取って最後まで仕上げる。中途半端なまま置かない。

---

## 8章 完了の定義

8.1 **公開まで終わって、はじめて完了。** 手元で動いただけでは完了ではない。

8.2 テストが通っただけでも完了ではない。**ユーザーの目に届いて、意図どおり見えて**完了。

8.3 できていないことがあるなら、隠さずそのまま報告する。

8.4 **完了を報告するときは、必ず版番号を書く**（例：「v0.1.4として公開しました」）。

　　理由：ユーザーはiPadの実機で確認している。手元の画面が新しい版に入れ替わったのか、
　　まだ古い画面が残っているのかを、**版番号でしか判断できない。**
　　版番号を伝えないと「直したと言われたのに直っていない」（実は古い画面を見ていた）
　　という無駄な行き来が起きる。

　　あわせて「画面右上の版番号がこれになっているか確認してください。
　　古いままならタブを閉じて開き直してください」と案内する。

8.5 記録だけの修正で、アプリ本体を変えていない場合は版番号を上げない（3.5）。
　　そのときも報告には「版番号は据え置き（v0.1.4のまま）」とはっきり書く。
　　黙っていると、ユーザーは新しい版が来るのを待ち続けてしまう。

---

## 9章 このアプリの技術構成（2026-09-02 確定）

9.1 素のJavaScript（ES2022）＋ESモジュール。**ビルドは使わない。**
　　Vite・React・TypeScript は使わない（1章の理由による。仕様書案から意図的に外した決定）。

9.2 外部の部品は **`vendor/` に実ファイルとして置いて** 使う。
　　npmでインストールしない。CDNから読み込まない。
　　理由：現場は通信が不安定。外部サーバーに頼ると、そこが止まった日にアプリが開かなくなる。

9.3 描画は **HTML5 Canvas の2D**。Three.js・WebGLは使わない。
　　2Dの図面に3Dの仕組みは要らず、印刷用に画像化する処理もCanvasのほうが素直に書ける。

9.4 画面の入口は `index.html` → `src/ui/app.js` の1つだけ。あとは `import` でつなぐ（2.1）。

9.5 ファイルの割り当て（1ファイル1役割。増やすときは司令塔が決める）

```
index.html            画面の入口
manifest.json         ホーム画面に追加するための設定
service-worker.js     オフライン用。04から流用（5.3の事故対策込み）
LICENSE               GPL-3.0（12章）
vendor/               同梱する外部部品（libredwg など）
src/version.js        版番号
src/drawing.js        図形データの形の定義と共通処理（範囲計算など）
src/dxf-parse.js      DXFの文字を読んで図形データにする
src/dwg-parse.js      DWGを（vendorの部品で）読んで図形データにする
src/viewport.js       図面の座標と画面の座標の変換、拡大縮小・移動の状態
src/render.js         Canvasに描く
src/measure.js        2点間の寸法を測る
src/notes.js          文字の追記
src/print-area.js     印刷用の絵づくり（大きさの計算と描き直し）
src/ui/print-ui.js    印刷する範囲を指で囲む画面
src/ui/print-preview.js  印刷される絵を、押す前に確認してもらう画面
src/ui/print-preview.css 上の見た目
src/ui/print-ui.css   上の見た目
src/ui/app.js         入口。全体のとりまとめ
src/ui/file-open.js   ファイルを選んで読み込む
src/ui/gestures.js    スワイプ・ピンチの受け取り
src/ui/toolbar.js     画面のボタン類
src/ui/drawing-list.js  覚えている図面から選ぶ画面
src/ui/drawing-list.css 上の見た目
src/ui/ui.css         見た目
tests/                テスト
tests/fixtures/       テスト用の見本図面（これだけはコミットしてよい）
```

---

## 10章 図形データの形（形式の違いをここで吸収する）

10.1 **DXFもDWGも、いったん同じ形の「図形データ」に変換してから描く。**
　　描画・印刷・寸法測定は、元の形式を一切知らない。

10.2 `dxf-parse.js` と `dwg-parse.js` は、**まったく同じ形** を返す。
　　この形は `src/drawing.js` に定義とコメントを書き、そこを唯一の決まりごととする。

10.3 形（v0.1.6時点）

```js
{
  units: 'mm',                         // 単位
  bounds: { minX, minY, maxX, maxY },  // 図面全体の範囲
  layers: [ { name, color } ],         // レイヤーの一覧（v1では表示切替はしない）
  entities: [
    { type:'line',     layer, color, x1, y1, x2, y2 },
    { type:'polyline', layer, color, points:[[x,y],...], closed },
    { type:'arc',      layer, color, cx, cy, r, startAngle, endAngle },
    { type:'circle',   layer, color, cx, cy, r },
    { type:'ellipse',  layer, color, cx, cy, rx, ry, rotation, startAngle, endAngle },
    { type:'text',     layer, color, x, y, height, rotation, text, hAlign, vAlign },
  ]
}
```

10.4 **ブロック（INSERT）は、読み込むときに展開して line / arc などに落とす。**
　　描画側に「入れ子」を持ち込まない。描画を単純に保つほうが事故が少ない。

10.5 対応していない図形に出会ったら、**黙って捨てない。**
　　数を数えて「◯個の図形は表示できませんでした」と画面に出す。
　　理由：図面が欠けて表示されたことに現場で気づけないのがいちばん危ない。

10.6 座標の向きに注意。CADのY軸は上向き、Canvasは下向き。
　　変換は `viewport.js` の1か所だけで行い、他のファイルでは絶対に反転させない。

---

## 11章 iPadでの動かし方

11.1 **SafariでもChromeでも見られるように作る。**

11.2 ただし **オフライン（ホーム画面に追加）はSafariでしか使えない。**
　　iPadのChromeは中身がSafariと同じ仕組みで動くため、ホーム画面への追加ができない。

11.3 そのため、Safari以外で開かれたときは
　　「電波の無い場所でも使うなら、Safariで開いてホーム画面に追加してください」
　　という案内を画面に出す。**専門用語は使わず、手順を絵か短い文で示す。**

11.4 指の操作は、必ず **実機のiPadで確かめる。** パソコンのマウスで動いても確認にならない。

11.5 誤操作対策：図面を見ているときに、指1本は「移動」、2本は「拡大縮小」。
　　寸法測定や範囲指定の最中は、モードを画面にはっきり出す（今どのモードか迷わせない）。

---

## 12章 使用許諾（GPL-3.0）

12.1 DWG読み込みに使う `libredwg-web` は **GPL-3.0** である。
　　これを同梱して配布するため、**このアプリ全体を GPL-3.0 で公開する。**

12.2 したがって、GitHubリポジトリは**公開（public）**にする。`LICENSE` を必ず置く。

12.3 **公開されるのはアプリのプログラムだけ。** お客様の図面ファイルは公開されない。
　　図面は端末の中だけで処理され、どこへも送信しない。この点はREADMEにも明記する。

12.4 GPL-3.0の部品は `vendor/` の中だけに置く。`src/` の中に混ぜない。
　　理由：後で「DWG対応をやめる」と判断したとき、`vendor/` と `src/dwg-parse.js` を
　　外すだけで済むようにしておくため。

12.5 **他の外部部品を足したくなったら、使用許諾を確認して司令塔に報告する。**
　　ワーカーが勝手に部品を足さない。

---

## 13章 iPadでファイルを選ぶときの決まり（実機で判明）

13.1 **ファイルを選ぶ入力欄に `accept` を付けない。**

　　`accept='.dxf,.dwg'` と書くと、**iPadのファイルアプリでDXFが灰色になって選べません。**
　　iPadは `accept` の拡張子を「iPadが知っているファイルの種類」に読み替えてから
　　絞り込みますが、`.dxf` `.dwg` は知らない種類なので読み替えに失敗し、
　　結果としてすべてのファイルが選べなくなります。

　　**便利に見えても付け直さないこと。現場で図面が開けなくなります。**

13.2 種類の判定は、**選ばれたあとに中身の先頭を見て**行う。
　　- DWG … 先頭が `AC10xx`
　　- DXF … 先頭のほうに `SECTION` が出てくる
　　拡張子が違っていても中身で判断できるので、現場で名前が変わっていても開けます。

13.3 隠したファイル選択欄は、`display:none` にも画面外（`left:-9999px`）にもしない。
　　**透明にして1ピクセルだけ置く。**
　　iPadでは、完全に消した入力欄を押しても選択画面が開かないことがあります。

13.4 **この種の不具合は、パソコンのブラウザでは絶対に再現しません。**
　　ファイルの読み込み・保存・共有まわりを変えたら、必ず実機のiPadで確かめること。

---

## 14章 画面の細かさ（拡大の倍率）の扱い

14.1 **描く倍率を1倍に決め打ちしない。**

　　iPadの画面は細かいので、キャンバスは「見た目の大きさ × 2倍」の点数で作られます。
　　ここを1倍と決めつけて描くと、**2倍の広さに1倍で描く**ことになり、
　　図面が画面の**左上4分の1にだけ縮こまります。** 実際に本番で起きました。

14.2 倍率を決める場所は **`render.js` の中の1か所だけ**にする。
　　`app.js` と `render.js` の両方が別々に設定すると、あとから動くほうが上書きして事故になる。
　　（実際、`app.js` が2倍に設定したあと `render.js` が1倍で上書きしていた）

14.3 呼び出す側が倍率を渡し忘れても正しく描けるよう、
　　`render.js` は**キャンバスの実際の点の数と見た目の大きさから自分で計算**する。

14.4 **この不具合はパソコンの画面（1倍）では絶対に再現しません。**
　　画面の見た目に関わる変更をしたら、`tests/render.test.js` のように
　　「Canvasのふりをする入れ物」で2倍・3倍の場合を必ずテストすること。

---

## 15章 CADで消してあるものを出さない

15.1 次のものは、CADの画面に出ないので、このアプリでも出さない。

　　- 図形ごとの「見えない」指定（グループコード60が1）
　　- 非表示（OFF）のレイヤー … LAYERテーブルの色番号がマイナス
　　- 凍結（FROZEN）のレイヤー … LAYERテーブルの70の1のビットが立っている

15.2 **なぜ大事か。**
　　AutoCADの「動的ブロック」は、ひとつの部品の中にありうる形を**全部まとめて持たせておき**、
　　「今回はこの形を使う」という指定で切り替えます。使わない形には「見えない」印が付きます。
　　この印を読まずに全部描くと、**切り替えたはずの形が全部重なって出てしまいます。**
　　参考図.dxf には「見えない」指定の図形が805個あり、実際に重なって出ていました。

15.3 消した図形は `unsupported`（表示できませんでした）に**数えない。**
　　もともと出さないものなので、数えると本当に足りない図形が埋もれます。
　　数だけ `drawing.hiddenCount` に記録して、あとで調べられるようにしておく。

15.4 レイヤー名は、登録するときも引くときも**前後の空白を取り除いてそろえる。**
　　片方だけだと、色も表示・非表示も引けなくなります。

---

## 16章 文字の向きとそろえ方

16.1 ふつうの文字（TEXT）は「何度傾ける」（コード50）で向きが決まる。

16.2 **MTEXT（長い文字）は、向きを矢印（コード11・21）で書くことが多い。**
　　これを読まずに50だけ見ると、**寸法の数字がいつも水平になります。** 実際にそうなっていた。
　　矢印があればそちらを優先し、無ければ50を使う。

16.3 文字を書き出す点の**どこに文字を置くか**（MTEXTのコード71）も読む。
　　寸法の数字は「中央のまん中」（5）で書かれているので、
　　左端から書くと**数字が寸法線からずれます。**
　　`drawing.js` の図形データに `hAlign` `vAlign` として持たせ、`render.js` が使う。

---

## 17章 ブロックの中身がレイヤー0のときの扱い（AutoCADの決まり）

17.1 **ブロック（部品）の中身がレイヤー「0」に描かれている場合、
　　その中身は「その部品を置いた側のレイヤー」に従う。**

　　これはAutoCADの決まりで、部品を色違いで使い回すための仕組みです。

17.2 実物の図面（参考図.dxf）のエルボがまさにこれでした。

　　- 部品を置いた場所 … レイヤー PIPE（赤）
　　- 部品の中身が描かれている場所 … レイヤー 0（黒）

　　この決まりを知らないと、**配管が赤いのにエルボだけ黒**になります。実際になりました。

17.3 **レイヤー0以外に描かれている中身は、そのレイヤーのまま。** ここまで置いた側に
　　従わせると、レイヤー分けが壊れます。やりすぎないこと。

17.4 色を直接指定してある図形は、その色のまま（置いた側のレイヤーに関係しない）。
　　「レイヤーに従う（BYLAYER）」と書かれているものだけが、17.1の対象です。

17.5 この決まりは、表示・非表示の判定（15章）にも同じように効きます。
　　実効レイヤーで判定すること。

---

## 18章 「表示できませんでした」に数えてよいもの・いけないもの

18.1 この案内は「**図面が欠けている**」ことを現場に知らせるためのもの（10.5）。
　　欠けていないものを数えると、**無用な心配をかけ、本当に足りない図形が埋もれる。**

18.2 **数えないもの**（図面の線ではない、またはもともと出ないもの）

| 種類 | 理由 |
| :--- | :--- |
| `VIEWPORT` | 印刷レイアウトの「のぞき窓」の設定。図面の線ではない |
| 寸法の部品の中の `POINT` | CADでも印刷されない目印（15.3） |
| 「見えない」指定・非表示/凍結レイヤーの図形 | もともとCADの画面に出ない（15章） |

18.3 **数えるもの**：`SPLINE` `HATCH` など、CADでは見えているのにこのアプリで
　　描けていない図形。ここは黙って捨てない。

18.4 新しい図形に対応したら、**その種類を数えるのをやめる**のを忘れない。
　　対応済みなのに数え続けると、案内がうそになる。

---

## 19章 楕円（ELLIPSE）の扱い

19.1 DXFでの書かれ方に注意。

　　- `11,21` は長いほうの軸の端だが、**中心からの相対位置**。絶対位置ではない
　　- `40` は「短い半径 ÷ 長い半径」の比
　　- `41,42`（どこからどこまで）は**ラジアン**。度に直すこと

19.2 **【落とし穴】始まりと終わりが同じ角度なら「ぐるっと一周」の意味。**

　　角度を0〜360度に直すと、一周の楕円は「0度→0度」になる。
　　そのまま描くと長さ0の弧になり、**何も描かれない。**
　　実物の図面には一周の楕円が100個以上あるので、必ず「0なら360」に直すこと。
　　同じ扱いが `drawing.js` の `ellipsePoints()` にも要る（範囲の計算で使う）。

19.3 範囲（bounds）は、輪郭の上の点をいくつか拾って求めている。
　　傾いた楕円をきっちり式で解くと複雑で、間違えたときに気づきにくいため。

---

## 20章 開いた図面を覚えておく（v0.1.7で追加）

20.1 **一度開いた図面は、アプリが覚えておく。**
　　アプリを更新するたびに、ファイルアプリから開き直させない。
　　現場でこれをやらせるのは、実害として重い。

20.2 覚えておくのは **元のファイルのバイト列（生のまま）**。
　　解析済みの図形データではない。

　　理由が2つある。
　　- 解析済みだと大きくなりすぎる（大きな図面では何十MBにもなる）
　　- **読み取りの作り（dxf-parse.js）を直したとき、覚えている図面にも自動で効く。**
　　　解析済みを覚えていると、古い読み方のまま残ってしまう

20.3 置き場所は **IndexedDB**。Service Worker のキャッシュとは別物として扱う（5.4）。
　　図面のデータをキャッシュ側に入れない。

20.4 **最大10件まで覚える。** いっぱいになったら古いものから消す。
　　起動時は、いちばん最近開いた図面を自動で開き直す。
　　（2026-09-03 ユーザー要望で5件→10件）

20.5 覚えられなかった場合（容量オーバー・プライベートブラウズなど）でも、
　　**アプリは普通に動くこと。** 覚える処理の失敗でアプリを止めない。

20.6 図面はこの端末の中だけに保存する。外へは一切送らない（12.3）。

---

## 21章 GitHub Pages の更新の届き方（実機で判明）

21.1 GitHub Pages は `Cache-Control: max-age=600` を返す。
　　**つまりブラウザは10分間、古いファイルを使い続ける。**

21.2 さらに悪いことに、**ファイルごとに別々にキャッシュされる。**
　　`app.js` は新しいのに `render.js` は古い、という**混ざった状態**が起こりうる。
　　これは版番号だけ見ても気づけないので危険。

21.3 対策：**Service Worker で「アプリ本体をまとめて1組」として扱う。**
　　- 1つでも取得に失敗したら、その更新は取り消す（混ざった状態を作らない）
　　- 新しい版を見つけたら画面に「更新があります」と出し、
　　　**ユーザーが押したときだけ切り替える**（5.2）

21.4 更新の案内には**新しい版番号を書く**（8.4と同じ理由）。

---

## 22章 更新にいつ気づくか（実機で判明）

22.1 **アプリが裏に回っている（他のアプリに切り替えた）間は、iPadが時計を止める。**
　　そのため「◯分ごとに更新を見に行く」だけでは、裏から戻ってきても新しい版に気づけない。
　　実際に「バックグラウンドで待機している間に更新が来ても気づかない」と報告があった。

22.2 対策：**表に戻ってきた瞬間に見に行く。** ここが実質いちばん効く。
　　見張るのは次の3つ（`src/update-check.js`）。
　　- `visibilitychange` … 他のアプリから戻ってきた／画面を点けた
　　- `focus` … パソコンで別のウィンドウから戻ってきた
　　- `pageshow` の `persisted` … ブラウザが冷凍保存していたページを復活させた

22.3 短い間に何度も見に行かないよう、最短の間隔（30秒）を決めておく。
　　画面の切り替えを繰り返したときに、通信を無駄にしないため。

---

## 23章 見えないものを「ある」と報告しない

23.1 **中身が空っぽの文字は、図形として作らない。**

　　DXFには、書式の指定（`\A1;` など）だけが入っていて、肝心の文字が無いMTEXTがある。
　　CADの画面には何も出ない。

23.2 これを図形として扱うと、次の実害が出る。

　　- **図面の遠くに「見えない文字」があることになり、
　　　「図面から遠く離れた場所に図形があります」と、見えないものを報告する。**
　　　ユーザーがCADで探しても見つからない（実際に探してもらったが見つからなかった）
　　- 見えない文字が図面の範囲を広げ、**図面が実際より小さく表示される**
　　　（参考図.dxf では 10639×5542 → 5441×4161 まで縮んだ。つまり2倍近く大きく表示できる）

23.3 案内は「◯個あります」だけで終わらせない。
　　**種類・中身・レイヤー名・座標まで書く。** そうしないとCADで探せない。

---

## 24章 覚えている図面の一覧（v0.1.9で追加）

24.1 **アプリの中で図面を切り替えられるようにする。**
　　ファイルアプリを経由せずに、覚えている図面から選んで開ける。
　　現場で何枚かの図面を行き来するため。

24.2 一覧に出すもの：ファイル名・いつ開いたか・大きさ。**新しい順。**

24.3 1件ずつ**消せる**ようにする。消すときは確認を出す
　　（現場で誤って消すと、電波が無いと取り戻せないため）。

24.4 今開いている図面が、一覧のどれか分かるようにする。

24.5 **覚えていないときは、一覧のボタンを出さない**（押しても何もないと混乱するため）。

---

## 25章 案内に出してよいこと・出してはいけないこと

25.1 **CADの画面に出ていないものを、「ある」と案内しない。**
　　現場の人はCADと見比べて確認する。CADに無いものを伝えると混乱するだけ。

25.2 【実際に起きたこと】
　　「図面から遠く離れた場所に2個の図形があります：文字『outlook』…」という案内を出した。
　　中身は正しかったが、**ユーザーにとっては使い道が無く、混乱させただけだった。**
　　図面本体に合わせて表示するという動作自体は正しいので、**動作は変えず、案内だけをやめた。**

25.3 案内に出すのは「**現場の判断に影響すること**」だけにする。

　　出す：`SPLINE` `HATCH` など、**CADでは見えているのにこのアプリで描けていない図形**
　　　　　（図面が欠けているので、現場で知らないと危ない）

　　出さない：
　　- 図面の外に置かれた図形（表示のしかたの話であって、図面は欠けていない）
　　- CADでも見えないもの（空の文字、印刷されない目印、非表示レイヤー）
　　- 図面の線ではないもの（印刷レイアウトののぞき窓）

25.4 迷ったら「**これを読んだ職人さんは、何をすればよいか分かるか**」で判断する。
　　何もすることが無いなら、出さない。

---

## 26章 範囲指定プリント（このアプリの最重要機能）

### 26.1 何をする機能か

画面上で**指で四角く囲んだ範囲だけ**を紙に印刷する。
現場で「この部分だけ持って行きたい」に応えるための、このアプリの一番の目的。

### 26.2 縮尺は「用紙に合わせる」だけ（2026-09-03 ユーザー決定）

**印刷した紙に定規を当てて測ることは無い**と確認済み。
そのため、囲んだ範囲が用紙いっぱいに収まるように自動で拡大縮小するだけにする。

**1/50 などの縮尺指定は作らない。** 使わない機能を作ると、その分だけ壊れやすくなる。
将来必要になったら、そのとき足す。

### 26.3 操作の流れ

1. ボタン「印刷する範囲」を押す → **範囲を囲むモード**に入る
2. 画面に「印刷したい範囲を指でなぞって囲んでください」と出す（今どのモードか迷わせない。11.5）
3. 指でなぞると、**半透明の四角**が出る。囲んだ外側は少し暗くして、中身をはっきりさせる
4. 指を離すと「**この範囲を印刷**」「やり直す」「やめる」の3つを出す
5. 「この範囲を印刷」で、iPadの標準の印刷画面を開く

### 26.4 範囲を囲んでいる間の指の扱い

**キャンバスの上に透明な板を1枚かぶせて、そこで指を受ける。**
こうしないと、図面を動かす操作（1本指のなぞり）と、範囲を囲む操作がぶつかる。

その間は図面の移動・拡大縮小はできない。
見る位置を変えたいときは、いったん「やめる」で抜けてもらう。
（同時にできるようにすると誤操作が増える。現場では単純なほうが安全）

### 26.5 印刷用の絵の作り方

- **画面に見えているものではなく、図面データから作り直す。**
  画面の絵をそのまま引き伸ばすと、線がぼやけて図面が読めなくなる。
- 大きさの目安：**印刷したときに1インチあたり200点**になるように計算する。
- **【重要】絵の大きさは「囲んだ範囲の実寸」ではなく「紙の大きさ」から決める。**
  用紙に合わせる方式では、最後に紙いっぱいへ引き伸ばすため、
  実寸で決めると**小さい範囲を囲んだときに紙の上でぼやける**（実測で71dpiになった）。
  「この部分だけ印刷したい」という、いちばんよくある使い方で困る。
- ただし**上限を必ず設ける**（iPadで大きすぎる絵を作ると、アプリが落ちる）。
  - 長いほうの辺：最大 4000点
  - 全体の点の数：最大 1200万点
  上限に当たったら、収まるまで小さくする（落とすより、少し粗いほうがまし）。
  **上限はiPadで落ちないための安全装置であって、当たらないのが正常。**
  紙の大きさから決めているかぎり、ふつうは当たらない。
- 線の太さは、印刷したとき **0.25mm 程度**になるように換算する。
  画面と同じ太さで描くと、紙の上では細すぎて見えない／太すぎて潰れる。

### 26.6 紙の向きと余白

- 囲んだ範囲が**横長なら横向き、縦長なら縦向き**の紙にする（自動）。
- 余白は 8mm。
- **図面以外は一切印刷しない。** ボタン・案内・版番号は紙に出さない。

### 26.7 失敗したときの扱い

- 絵を作るのに失敗したら（容量不足など）、**日本語で理由を出す。**
  黙って何も起きないのがいちばん困る。
- 範囲が小さすぎるとき（指が滑っただけのとき）は印刷させない。
  目安：短いほうの辺が画面上で 20 ピクセル未満なら「範囲が小さすぎます」と出す。

### 26.8 作らないもの（今回は）

- 縮尺の指定（26.2）
- 複数ページに分けて印刷
- 図面名や日付の印字（**紙には図面だけ**。必要になったら足す）

---

## 27章 印刷の落とし穴（実際に紙へ出して判明）

2026-09-04、ユーザーが**実物の紙をスキャンして見せてくれた**ことで、
画面では絶対に気づけない不具合が3つ見つかった。

27.1 **`window.print()` の直後に絵を片付けてはいけない。**

　　iPadのSafariでは `window.print()` が**すぐに戻ってくる**（印刷画面は後から開く）。
　　直後に片付けると、**印刷される前に絵が消えて紙が白紙になる。**
　　実際に、図面ではなく「印刷する図面」という代わりの文字だけが印刷された。

　　パソコンでは `print()` が印刷画面を閉じるまで待つので、
　　**この不具合はパソコンでは絶対に再現しない。**

　　片付けは `afterprint`（印刷が終わった合図）で行う。
　　合図が来ない環境のために、長めの時間切れも置く。

27.2 **紙の余白（`@page` の margin）は 0 にする。**

　　余白があると、ブラウザがそこに**ページのURL・日付・ページ番号**を勝手に入れる。
　　実際の紙に、アプリのURLと `2026/09/04 6:50` `1/1ページ` が印刷されていた。
　　現場に渡す図面に、そんなものは要らない。

　　余白を0にすると入れる場所が無くなるので出なくなる。
　　図面が紙の端にくっつかないよう、**余白は入れ物の内側（padding）で取る。**

27.3 **`decode()` を時間切れなしで待ってはいけない。**

　　`decode()` は「もう描ける状態」まで待ってくれる便利な機能だが、
　　**画面に出していない絵（`display:none`）では終わらない。**
　　印刷用の絵はふだん画面に出していないので、まさにこれに当たる。
　　時間切れを付けずに待った結果、**印刷ボタンを押しても何も起きなくなった。**

　　先に「読み込みが終わったか」（`complete` と `naturalWidth`）を確かめ、
　　`decode()` は時間切れ付きで待つ。終わらなければそのまま印刷してよい。

27.4 **印刷は、必ず実物の紙で確かめてもらう。**

　　画面での確認と、テストと、紙に出た結果は**別物**。
　　上の3つは、どれも画面では正常に見えていた。

---

## 28章 印刷のやり方を変える（2026-09-04 ユーザー了承）

### 28.1 なぜ変えるか

27章の直し方では**まだ紙が正しく出なかった。** 実機で分かったこと：

- 印刷画面で**紙の向きを変えると、ページが作り直される**
- そのとき「印刷が終わった」合図が飛び、後片付けが走って**絵が消える**
- 一度消えると、次に印刷しようとしても**二度と絵が出ない**

**印刷中に後片付けをする設計そのものが間違いだった。**

さらに調べたところ、**iPadのSafariは紙の余白にURLと日付を入れるのを、
CSSでは止められない**（`@page { margin: 0 }` は効かない）。

### 28.2 新しいやり方

**ページを印刷するのをやめ、「図面の絵そのもの」をiPadに渡す。**

1. 範囲を囲む（ここまでは同じ）
2. 「この範囲を印刷」を押す
3. **アプリの中に、印刷される絵をそのまま大きく出す**（確認できる）
4. 「プリント」を押すと、iPadの共有メニューが開く
5. そこから「プリント」を選ぶ

こうすると：

- **URL・日付・ページ番号は一切出ない**（ページを印刷しないため）
- **白紙になる危険が無い**（絵を先に作って、目で見てから渡す）
- **印刷される絵を、押す前に確認できる**（現場で紙を無駄にしない）

### 28.3 気をつけること

- **絵は消さない。** 印刷中に片付けようとしない（これが27章の失敗）。
  次に印刷するときに入れ替わるので、消す必要がそもそも無い。
- 共有メニューを開く処理は、**指で押した流れの中で呼ぶ**。
  iPadは、間に待ち時間が入ると共有メニューを開かせないことがある。
  そのため**絵は先に作っておき、ボタンを押した瞬間に渡すだけ**にする。
- 共有メニューが使えない場合（パソコンなど）は、これまでどおり印刷画面を開く。
- 共有が断られた・失敗したときは、**日本語で次にどうすればよいかを出す。**

### 28.4 作らないもの

- ページを印刷する方式（`window.print()`）は、**パソコン用の代わりの道**としてだけ残す。
  iPadでは使わない。

---

## 29章 印刷する絵は「A4用紙1枚そのもの」にする（2026-09-04 実物の紙で判明）

### 29.1 何が起きたか

28章のやり方（絵をiPadに渡して印刷する）で、URLと日付は消えた。
ところが実物の紙を見ると、**図面の左右が切れていた。**

ユーザーの報告：「横が切れている」

### 29.2 なぜ切れたのか

v0.2.2 までは、**囲んだ範囲の形をした絵**を作って渡していた。
たとえば横長に囲めば、横長の細長い絵ができる。A4用紙とは形が違う。

iPadのプリント画面は、渡された絵を**紙いっぱいに引き伸ばす。**
絵と紙の形が違うと、そのとき絵が紙からはみ出す。

そして**プリンターは、紙のいちばん外側（ふつう上下左右3〜5mm）には
物理的に印刷できない。** はみ出した部分がちょうどそこに来て、
図面の左右が切れて出てきた。

**画面の確認画面ではまったく分からない。紙に出して初めて分かる。**

### 29.3 直し方

**絵そのものを、A4用紙1枚の形にする。**

- 絵の大きさ ＝ A4用紙1枚（200dpiで 2338×1653点。約386万点）
- 絵の**外周8mmは白い余白**。図面はその内側にだけ描く
- 絵と紙の形が同じなので、引き伸ばされても位置がずれない
- 紙の縁で失われるのは**白い余白だけ**。図面は絶対に切れない

これで、プリント画面の設定は**標準のままでよくなった。**
紙の向きも拡大率も、ユーザーが触る必要がない
（触らせないほうが事故が減る。28.1の不具合も紙の向きを変えたことが引き金だった）。

### 29.4 いっしょに直したこと

**(a) 囲んだ範囲の外は描かない。**

描く先が紙1枚ぶんに広がったので、何もしないと**囲んだ範囲の外にある図形**まで
白い余白に描かれてしまう。それでは「この範囲を印刷」と言いながら範囲外のものが出るし、
せっかくの余白に線が来て、また紙の縁で切れる。
囲まれた範囲の四角の中だけに描く（`ctx.clip()`）。

**(b) 余白を二重に取らない。**

絵の中にすでに8mmの余白があるので、CSS側（`.print-area` の `padding`）は0にする。
両方で取ると図面がむだに小さく印刷される。

**(c) 画面用の「まわり5%のすき間」を打ち消す。**

`fitToBounds()` は画面で見やすいよう、まわりに5%のすき間を付ける。
そのまま紙に使うと、A4横で左右が8mmではなく**20.8mm**になった（実測）。
紙では打ち消して、余白は8mmだけにする。

**(d) 確認画面で紙ぜんたいが見えるようにする。**

確認画面の箱（`.pv-box`）の高さが `max-height` だけだと「中身しだい」のままで、
中の絵の `max-height:100%` が効かず、**紙の下のほうが見切れていた。**
`height: 100%` を足して高さをはっきりさせる。
見切れていては、印刷前の確認にならない。

### 29.5 決まり

- **印刷用の絵は、必ず紙と同じ形にする。** 中身の形に合わせてはいけない。
- **紙の縁3〜5mmには印刷できない**前提で作る。余白は絵の中に持たせる。
- 余白は**1か所でだけ**取る。絵の中とCSSの両方で取らない。
- 印刷は、必ず**実物の紙**で確かめてもらう（27.4）。
  今回も、画面の確認画面ではまったく分からなかった。

---

## 30章 囲んだ範囲を、あとから調整できるようにする（2026-09-04 ユーザー要望）

### 30.1 なぜ要るか

> ユーザー要望：「印刷範囲を指定したときに、その指定を調整できるような機能をつけて欲しい」

一度で思いどおりに囲めることは、まずない。とくに現場で手袋をした指では難しい。
それまでは「囲み直す」しかなく、**少しずれただけでも最初からやり直し**だった。

### 30.2 作ったもの

囲み終わったあと（ボタンが出ている状態）で、

- **四角のまわりの丸（つまみ）をつまむ** → 大きさを直せる
- **四角の中を押したまま動かす** → 範囲ごと動かせる
- 「囲み直す」で最初から囲み直すこともできる（今までどおり）

つまみは、角に4つ・辺の真ん中に4つ。

### 30.3 気をつけたこと

**(a) つまみは指で押せる大きさにする。**

見た目の丸は26pxだが、**当たり判定は44px以上**にしてある（11章）。
手袋をした指でもつまめるように、透明な広い当たり判定を丸のまわりに付けている。
`print-ui.css` の `.pr-handle::before` と、`print-ui.js` の `HANDLE_HIT_PX` を
同じ値に保つこと。

**(b) 四角が小さいときは、辺の真ん中のつまみを出さない。**

小さい四角で8つ全部出すと丸どうしが重なり、どれを押したのか分からなくなる。
角の4つは、どんなに小さくても必ず出す。

**(c) ボタンの箱が、つまみを隠さないようにする。**

四角が画面の下のほうにあると、ボタンの箱が下のつまみに重なる。
重なると指が箱に取られ、**下の辺を動かせなくなる。**
そのときは箱を上へ移す。

　　**箱の高さを決め打ちで見積もってはいけない。**
　　最初「高さは190pxくらい」と見積もったところ、実測187pxで数ピクセル足りず、
　　**判定を外して実際に隠れた**（実機のブラウザで確認）。
　　**実際に置いてみて、本当に重なるかを測る**こと。

**(d) つまんだまま反対側へ行っても壊れないようにする。**

左のつまみを右辺より右へ引っぱったら、左右が入れ替わるだけにする。
幅が負になって表示が壊れる、といったことが起きないようにする。

**(e) 小さくつぶせないようにする。**

つぶれた範囲は印刷できない（26.7）。つまみで作れてしまってはいけない。
`MIN_RECT_PX`（28px）より小さくはならない。

**(f) 画面（キャンバス）の外へ出さない。**

大きさを直すときも、範囲ごと動かすときも、四角は画面の中に留める。

**(g) 調整の途中で指が外れても、それまでの範囲を消さない。**

消してしまうと、また一から囲み直しになる。
「なぞって囲んでいる最中」の中断とは分けて扱う。

**(h) つまみは、なぞっている最中には出さない。**

囲んでいる最中に丸が出ると、指の下でちらついて分かりにくい。

### 30.4 試験のしかた

大きさや位置を決める計算は、**画面に触らないふつうの関数**として
`src/ui/print-ui.js` から取り出してある
（`handlePositions` `visibleHandles` `hitTestHandle` `isInsideRect`
`resizeRect` `moveRect`）。
画面が無いnodeでもそのまま試せる。画面の作りに関わる部分だけ、
ソースを読んで確かめている（`tests/print-ui.test.js`）。

---

## 31章 ボタンを増やさない・要らないものは外す（2026-09-04 ユーザー判断）

> ユーザー判断：「拡大縮小のボタン、これは必要ないと思うので削除しておいてください」

**「拡大」「縮小」ボタンを外した。** ボタンは4つになった。

　　図面を開く／図面を選ぶ／全体表示／印刷する範囲

理由：iPadでは**2本指のつまむ操作（ピンチ）で拡大縮小できる**ので、
同じことをするボタンは要らない。ボタンが減ったぶん、
残ったボタンを大きく・押し間違えにくくできる。

**拡大縮小そのものは無くなっていない。** `src/ui/gestures.js` が受け持つ。

決まり：

- **ボタンは増やさない。** 指で直接できることに、ボタンを足さない。
- 外すときは**残りかすを残さない。** 押しても何も起きないボタンや、
  呼ばれない処理が残っていると、あとで読む人が混乱する。
  `tests/print-ui.test.js` に、残りかすが無いことを確かめる試験を置いた。

---

## 32章 範囲を囲んでいる間も、図面を拡大縮小できるようにする（2026-09-04 ユーザー要望）

### 32.1 なぜ要るか

> ユーザー要望：「この範囲指定をしているときに拡大縮小ができない。
> ピンチアウト・ピンチができないので、これを行えるようにしてください」

「印刷する範囲」に入ると、キャンバスの上に透明な板（`.pr-board`）をかぶせて指を受ける
（26.4）。この板が指をぜんぶ受け取ってしまうので、
図面を動かす係（`gestures.js`）に指が届かず、**拡大縮小がまったくできなかった。**

細かい部分を印刷したいときほど、拡大して確かめながら囲みたい。
いちど範囲指定から抜けて、拡大して、また入り直す——では現場で使えない。

### 32.2 どう分けたか

板の上の指を、本数で分ける。

- **指1本** … 範囲を囲む・つまみで直す・範囲ごと動かす（今までどおり）
- **指2本** … 図面そのものを拡大縮小・移動する（ピンチ）

パソコンでは、マウスのホイールでも拡大縮小できるようにした（`gestures.js` と同じ）。

**【いちばん大事：四角は図面に貼り付けたまま動かす】**

四角は画面の座標で持っている。何もしないと、**図面だけが動いて四角は画面に取り残される。**
すると「囲んだつもりの場所」と「実際に印刷される場所」がずれる。
紙に出すまで気づけない、いちばんたちの悪いずれ方をする。

そこで、図面を動かす前に四角を**図面の座標**へ直し、動かしたあとで画面の座標へ戻す。
こうすると、拡大しても縮小しても、四角は図面の同じ場所を指し続ける。

　　図面の座標との行き来は **viewport.js だけの仕事**（10.6）。
　　`print-ui.js` に計算させてはいけない。自前でY軸をひっくり返すと、
　　直す場所が2か所になって必ず食い違う。
　　`app.js` から `toDrawing` / `toScreen` を渡し、`print-ui.js` はそれを呼ぶだけにする。

### 32.3 「範囲が小さすぎます」の測り方を直した

範囲の大きさは、**囲んだときの大きさ**で測る。

図面ぜんたいを見ようと縮小してから印刷すると、四角は画面の上では小さく見える。
そのときの大きさで測ると「範囲が小さすぎます」と断ってしまう。
**範囲そのものは何も変わっていないのに、である。**

`print-ui.js` が「指で囲んだ・つまんで直した直後の大きさ」を覚えておき、
印刷のときにそれを渡す。拡大縮小では更新しない。

### 32.4 気をつけたこと

- **2本目の指が触れたら、1本指でやりかけていたことは、なかったことにする。**
  つまみを持った指のまま2本目を置いたとき、つまみが動いたままにならないよう、
  つまむ前の形へ戻してから拡大縮小に移る。
- **囲みかけ（まだ確定していない四角）も、2本目の指で捨てる。**
  拡大したいだけなのに、細い四角ができてしまうため。
- **2本→1本に減っても、残った指で囲み始めない。** 指を全部離すまで待つ。
- **モードから抜けるときは、おさえている指の記録も空に戻す。**
  残ると「触っていないのに図面が動き続ける」不具合になる（`gestures.js` と同じ事故対策）。
- 2本指で拡大縮小できることは、**画面に文字で出す。**
  出していないと、そもそもできることに気づかない。

### 32.5 試験のしかた

`pinchMidpoint` `pinchDistance` `rectToDrawing` `rectFromDrawing` を
**画面に触らないふつうの関数**として切り出し、nodeで試験している。
とくに「拡大しても、四角は図面の同じ場所を指し続ける」は、
上下が逆（Y軸の反転）を取り違えるとすぐ壊れるので、必ず試験を通すこと。

つなぎ込み（本物の `viewport.js` と本物の `print-ui.js` を合わせた動き）は、
ブラウザで指2本の操作を流し込んで確かめた。
**拡大したあと、四角の図面上の位置のずれが 0 であること**を実測している。

---

## 33章 パソコンでも図面を動かせるようにする（2026-09-04 実機で判明）

### 33.1 何が起きたか

> ユーザー報告：「iPadではすべてOK。パソコンChromeでは拡大縮小はいいが移動できない。
> iPadでは指2本で動かせる」

32章で「指2本＝図面を動かす」にしたが、**パソコンには指2本が無い。**

- 拡大縮小 … マウスのホイールを回す → できていた
- 移動 … **手が1つも無かった**

マウスの左ボタンで引っぱるのは「範囲を囲む」に使っている。
つまりパソコンでは、囲んだあとに図面をずらすことができなかった。

**iPadだけで確かめていたら見つからない不具合だった。**
指2本の代わりを用意し忘れた、というだけの話だが、
片方の機械でしか試さないと、こういう抜けは必ず残る。

### 33.2 直し方

パソコンでは、次の2つで図面を動かせるようにした。

- **ホイールボタンを押したまま動かす**（AutoCADと同じ操作。CADを使う人にはこれが自然）
- **Shiftキーを押しながら動かす**（ホイールボタンの無いマウス・トラックパッド用）

拡大縮小はホイールを回す（今までどおり）。
**左ボタンだけで引っぱるのは「範囲を囲む」のまま**。ここを変えると囲めなくなる。

あわせて、右ボタンなどで範囲を囲み始めないようにした。

### 33.3 案内の文は、機械に合わせて出し分ける

パソコンの人に「2本指で」と書いても分からない。
iPadの人に「ホイールで」と書いても分からない。

`(pointer: coarse)` で見分けて、案内の文を変える。

- 指で触る機械 … 「2本指で図面を拡大・縮小できます」
- パソコン … 「ホイールで拡大縮小、ホイールを押したまま（またはShiftを押しながら）動かすと移動できます」

見分けに失敗する環境（`matchMedia` が無い・例外を投げる）では、
**指で触る機械として扱う。** このアプリはiPadが主なため。

### 33.4 気をつけたこと

- **マウスで動かすときも、四角は図面に貼り付けたまま**にする（32.2と同じ）。
  ここを忘れると、動かしたぶんだけ印刷される場所がずれる。
- 図面を動かし終わったときの後始末は、**1か所にまとめる**（`endViewGesture`）。
  ピンチとマウスで別々に書くと、片方だけ直し忘れる。
- ホイールボタンを押したときは `preventDefault()` する。
  しないとブラウザの自動スクロールが始まってしまう。

### 33.5 決まり

**iPadとパソコンの両方で確かめる。** 開発ルール7.3の「実物を見る」は、
**機械ごとに**必要になる。片方で動いても、もう片方に同じ手があるとは限らない。

- 指2本の操作を足したら、**パソコンでの代わりの手**も同時に用意する
- 逆に、キーボードやマウス前提の操作を足したら、**指だけでできる手**も用意する

---

## 34章 全部のレイヤーが「消してある」図面（2026-09-04 DWG対応で判明）

### 34.1 何が起きたか

DWGをDXFに変換して読ませたところ、**図形が1つも表示されなかった**（真っ白）。
レイヤーは51個ちゃんと読めているのに、図形が0個。

原因は**レイヤーの色番号の符号**だった。

DXFでは、レイヤーの色番号（グループコード62）が**マイナスなら「そのレイヤーは非表示」**
という決まりがある。ところがDWGから変換したDXFでは、**全部のレイヤーがマイナス**で出てくる。

同じ図面（参考図）で実際に比べた結果：

| | レイヤー`0` | `DIM` | `PIPE` | `既設PIPE` |
| --- | --- | --- | --- | --- |
| AutoCADが書き出したDXF | `7` | `3` | `1` | `2` |
| DWGから変換したDXF | `-7` | `-3` | `-1` | `-2` |

**色の値は同じで、符号だけが違う。**
決まりどおりに読むと「全部のレイヤーが非表示」となり、図形が全部消える。

### 34.2 直し方

**全部のレイヤーが非表示、という図面はありえない**（何も映らない紙と同じ）。
そういうときは、マイナスの符号は当てにならないと判断して、表示扱いに戻す。

- **一部だけ**マイナスなら、それは本当にCADで消してあるレイヤー。**そのまま尊重する。**
- **全部**マイナスなら、その符号には情報が無い。**全部を表示扱いにする。**
- 凍結（FROZEN・コード70の1のビット）は**別の情報**なので、こちらは必ず尊重する。

### 34.3 気をつけること

- **図形を組み立てる前に直すこと。**
  あとから直しても、いちど消された図形は戻らない。
- この直しは、DWGだけでなくDXFにも効く。
  **「全部消えている」より「全部出ている」ほうが、まだましである**（真っ白事故を防ぐ）。
- 直しが効きすぎて、**本当に消してあるレイヤーまで出してはいけない。**
  見本の図面を2つ（全部消し／一部消し）用意して、両方をテストで押さえてある。

---

## 35章 DWGを開けるようにする（2026-09-04）

### 35.1 やり方：DWGをDXFに変換して、同じ道に合流させる

```text
DXFファイル ──────────────────────┐
                                  ├→ dxf-parse.js → 図形データ → 表示・印刷
DWGファイル → [LibreDWG] → DXF ──┘
```

**DWGを読み解く係を、別に作らない。**

理由：`dxf-parse.js` には実物の図面で見つけた直しがたくさん入っている
（文字コードの見分け・ブロックの中の色・寸法の文字の向き・見えない図形・楕円…）。
DWG用にもう1つ読み解く係を作ると、**同じ直しを2か所でやることになり、
必ずどちらかが古くなって食い違う。**

この作りが正しいことは、実物で確かめた。同じ図面（参考図）について：

| | AutoCADのDXF | DWGから変換 |
| --- | --- | --- |
| 図形の数 | 602 | **602** |
| 線／円／円弧／楕円／文字 | 482／52／13／2／52 | **同じ** |
| 色ごとの数 | 黒386 赤144 … | **同じ** |
| 図面の範囲 | -88.41〜5352.33 | **同じ** |
| 表示できなかったもの | HATCH 1・POINT 1 | **同じ** |

**すべて一致した。**

### 35.2 GPLの部品に触れるのは1ファイルだけ

変換に使う LibreDWG は **GPL-3.0**。同梱して配るので、このアプリ全体もGPL-3.0にしている。

GPLの部品に触れるのは `src/dwg-parse.js` **だけ**にする。
DWG対応をやめるときは、次の2つを消せばよい。

- `src/dwg-parse.js`
- `vendor/libredwg/`

出どころ・版・使用許諾は `vendor/libredwg/README.md` に書き残すこと。

### 35.3 10MBの部品は、別枠でしまう

DWGを読む部品は**約10MB**ある。アプリ本体は全部あわせて0.27MBしかない。
これをいっしょに事前キャッシュすると、2つの困ったことが起きる。

1. **アプリを更新するたびに10MBを読み直す**（キャッシュ名に版番号が入っているため）
2. 事前キャッシュは「1つでも失敗したら全部やり直し」の作り（21.3）。
   10MBの読み込みが1回でも途切れると、**アプリの更新そのものが止まる**

そこで、こう分けた。

- 事前キャッシュ（`dxf-viewer-shell-<版>`）… **アプリ本体だけ。0.27MB。**
- 部品の枠（`dxf-viewer-dwg-engine-<部品の版>`）… **10MB。DWGを開いたときに入る。**

**部品の枠は、アプリを更新しても消さない。** 消すと10MBの読み直しになる。
片付けるのは、部品そのものを新しくしたときだけ。

しまうのに失敗しても（容量オーバーなど）、**その回の表示は成功させる。**
しまえなかっただけで、今見たい図面まで巻き添えにしない。

### 35.4 待たせるときは、何を待っているのか出す

10MBの読み込みは、電波の状況によっては何秒もかかる。
黙って止まっていると壊れたように見えるので、画面に出す。

- 「DWGを読む部品を用意しています（初回のみ・約10MB）…」
- 「DWGを読み込んでいます…」

### 35.5 決まり

- **DWGは必ずDXFに変換してから読む。** 読み解く係を2つ作らない。
- **GPLの部品に触れるのは `src/dwg-parse.js` だけ。**
- **同梱した部品は、出どころ・版・使用許諾をREADMEに書き残す。**
- **`.wasm` は `.gitattributes` に `binary` を入れる。**
  改行コードを変換されると1バイト変わって動かなくなる。
- **10MBの部品を事前キャッシュに入れない。** アプリの更新が止まる。
- はじめてDWGを開くときだけ電波が要ることを、READMEに書いておく。

---

## 36章 印刷は「絵」ではなく「PDF」で渡す（2026-09-04 ユーザーの指摘で判明）

### 36.1 なぜ変えたか

> ユーザーの指摘：「このプリントはPNG映像で出力印刷していますか。
> もしそうならプリンターが遅いのはそのせいですねえ。一回pdfに落とす事は可能ですか」

**そのとおりだった。**

29章で作った「紙1枚ぶんの絵（PNG）」は、2338×1653＝**約387万点の集まり**である。
プリンターは紙ぜんたいを点で塗ることになり、印刷にとても時間がかかっていた。

PDFなら「ここからここまで線を引く」という**命令の並び**で渡せる。
プリンターは自分の解像度で線を引くだけなので速い。

同じ範囲で実測：

| | 中身 | 大きさ |
| --- | --- | --- |
| 絵（PNG） | 約387万点 | 205KB |
| PDF | 線393本・曲線163・文字27 | **26KB** |

おまけに、**どんな解像度のプリンターでも線がぼやけない**（点の数に縛られない）。
それまでは200dpiの点の集まりだったので、600dpiのプリンターでも200dpi相当だった。

### 36.2 紙の上の置き方は、絵とPDFで必ず同じにする

置き方が食い違うと、**確認画面では合っているのに紙だけずれる。**
紙に出すまで気づけない、いちばんたちの悪いずれ方になる。

そこで置き方の計算は `src/print-area.js` の **`computePrintPlacement()` 1か所**に置き、
絵もPDFも同じものを使う。PDF側で紙の大きさや余白を持ち直してはいけない。

**上下をひっくり返さないこと。** CADもPDFも「上へ行くほどYが大きい」。
上下を入れ替えるのはCanvasに描くときだけで、それは viewport.js の仕事（10.6）。
取り違えると**図面がさかさまに印刷される**。
テストで、PDFの中の座標そのものを見て押さえてある。

### 36.3 囲んだ範囲の外の図形は、PDFに入れない

範囲の外は切り取られて紙には出ない。それなのに入れると、
ファイルが重くなり、作るのにも時間がかかる。

とくに漢字は下の36.4のとおり「型（絵）」にするので、**1つで数百KBになりうる。**
実際、狭く囲んだときに2MBのPDFができてしまい、そこで気づいた。

判定はざっくり（図形を囲む四角どうしが重なるか）でよいが、
**迷ったら「かかっている」側に倒すこと。**
消してはいけないものを消すより、余分に持っていくほうがはるかに安全。

### 36.4 文字の扱い

| 文字 | やり方 |
| --- | --- |
| 数字・英字・記号（`45°` の度記号も含む） | PDFに元から入っている書体（Helvetica）で書く。線と同じでぼやけない |
| 漢字・かな | PDFの標準の書体に入っていない。**その文字だけ小さな白黒の型にして貼る** |

実物の図面（参考図）で数えたところ、文字52個のうち漢字を含むのは12個
（「北 南 東 西 上 下 北東…」の方角）だけだった。この方法で十分に軽い。

型は `/ImageMask` として貼る。黒い点のところだけが色で塗られ、
白いところは透けるので**下の線を隠さない。**
データはそのままのバイトで入れ、同じ値が続くところをまとめて縮める
（`/RunLengthDecode`。まっ白が続くので何十分の一にもなる）。

**漢字の型が作れないときは、PDFをあきらめて絵で印刷する。**
黙って文字を落とすと、**現場で方角の分からない図面**が出てしまう。

### 36.5 決まり

- **PDFが作れなかったら、絵（PNG）で印刷する。** 印刷できないより、重くても印刷できるほうがよい。
- **PDF作りに待ち時間（await）を入れない。** 「プリント」を押した流れの中で渡すため（28.3）。
- **確認画面は絵のまま**でよい。ただし置き方は同じでなければならない（36.2）。
- 紙の大きさ・余白・線の太さは `print-area.js` から取る。PDF側で持たない。
- PDFを直したら、**必ず本物のPDFビューアで開いて目で確かめる。**
  形式が少しでも狂うと「壊れています」となって、まったく開けない。

---

## 37章 印刷のやり方は、機械で分ける（2026-09-04 実機で判明）

### 37.1 何が起きたか

> ユーザー報告：「iPadの方ではすべて正常です。プリントも通常通りの速さに戻りました。
> それからpcのほう、これは共有メニューにプリント項目はありません。
> 以前のような形式のほうがいいんですけど」

28章から「作ったファイルを共有メニューへ渡す」やり方にしていた。
iPadではこれで正しい。共有メニューに「プリント」があるからである。

**ところがWindowsのブラウザにも共有メニューはあり、その中に「プリント」が無い。**
出るのはメールや近くの人への送信だけ。渡しても印刷できなかった。

### 37.2 見分け方を間違えていた

それまでの判定はこうだった。

```js
if (blob && typeof navigator.share === 'function') { /* 共有メニューへ */ }
```

これは「**共有できるか**」を見ている。だがここで知りたいのは
「**共有メニューにプリントがあるか**」であって、同じではない。

正しくは「iPad（iPhone）かどうか」で分ける。

| 機械 | 印刷のやり方 |
| --- | --- |
| iPad・iPhone | 共有メニューへ渡す。そこから「プリント」を選んでもらう |
| パソコン | **ブラウザの印刷画面を直接開く** |

見分けは `src/ui/device.js` の `isApplePrintShareDevice()` 1か所に置く。
**iPadOSはUAが「Macintosh」と名乗る**ので、名前だけでは見分けられない。
指で触れる点の数（`maxTouchPoints`）と合わせて見るのが定石。

　　**教訓：できるかどうか（機能があるか）と、
　　それで目的が果たせるかは別物。** 機能の有無だけで道を分けない。

### 37.3 パソコンでの印刷のしかた

見えない入れ物（iframe）にPDFを読み込ませ、その中で印刷する。

- ページを印刷するのではないので、**URLも日付も入らない**（26章の問題が起きない）
- 渡すのは36章で作ったPDFそのもの。**iPadと同じものが出る**
- 押したらすぐ印刷画面が出る（ユーザーの希望どおり）

**【28.3】印刷中に片付けようとしないこと。**
入れ物は置いたままにして、次に印刷するときに中身を入れ替える。
片付けると、印刷画面で設定を変えたときに中身が消える。

印刷画面を開けなかったときは、**PDFを新しいタブに出す。**
何も起きないのがいちばん困る（26.7）。

### 37.4 「PDFで保存」を足した

> ユーザー要望：「これならpdf出力にも対応できるのではないですか」
> 「pdfで保存という機能追加して下さい」

PDFはもう作ってあるので、保存はそれを渡すだけで済む。
確認画面に「PDFで保存」を足した。

- iPadのSafari … 「ファイル」アプリに保存される
- パソコン … ダウンロードのフォルダーに入る

気をつけること：

- **押した流れの中で終わらせる。** 待ち時間を入れると、ブラウザが
  「勝手なダウンロード」とみなして止めることがある（28.3と同じ理由）。
- **すぐに片付けない。** 保存が終わる前に消すと、途中で切れることがある。
- ボタンの名前は**実際に保存されるものに合わせる。**
  PDFが作れず絵になったときに「PDFで保存」と出したら、うそになる。

### 37.5 決まり

- **「その機能があるか」で道を分けない。「それで目的が果たせるか」で分ける。**
- 機械の見分けは `src/ui/device.js` の1か所だけ。あちこちに書かない。
- iPadとパソコンの**両方**で確かめる（33.5）。片方で動いても、もう片方は別の道を通る。

---

## 38章 ハッチング（斜線）と点に対応する（2026-09-04）

### 38.1 何を直したか

実物の図面（参考図）で、こう出ていた。

> この図面のうち 2個の図形は表示できませんでした（種類：HATCH 1個、POINT 1個）

この2つに対応して、**「表示できませんでした」を0にした。**

### 38.2 ハッチングは、ふつうの線に直す

ハッチング（HATCH）は「この範囲を斜線で埋める」という指定。断面や壁を表すのに使う。

**新しい図形の種類は増やさない。** 囲いと模様を読み取って、
**ふつうの直線の並びに直してしまう**（`src/hatch.js`）。

　　種類を増やすと、画面・PDF・範囲の計算・切り取り判定…と
　　直す場所が一気に増え、どこかを忘れる。
　　線にしてしまえば、あとは今までの道をそのまま通る。

やり方：

1. 囲い（境界）を、点の並び（多角形）にする。円弧・楕円弧・ふくらみもここで直線に直す
2. 模様の決まりから、囲い全体を覆うだけの平行線を並べる
3. 1本ずつ、**囲いの中に入っている部分だけ**を切り出す

切り出しは「線が囲いの辺を何回またいだか」で決める（偶数なら外、奇数なら中）。
CADのハッチングも同じ考え方なので、**穴のあいた形（ドーナツ）も正しく抜ける。**

### 38.3 気をつけたこと

**(a) 模様の数字は、そのまま使う。**

HATCHの中の模様の角度・間隔は、**すでに拡大率と回転を掛けたあとの値**である
（実物の図面で確認した。コード52が630度、41が24なのに、模様の角度は225度だった）。
コード52や41を重ねて掛けると、模様が壊れる。

**(b) 基準の点は、遠くにあることがある。**

実物では、模様の基準の点が図形から1万以上離れていた。
基準から順に線を数えていくと、いつまでも囲いにたどり着かない。
**囲いの位置から何本目かを先に計算して**、そこから並べる。

**(c) べた塗りは、囲いの形だけ描く。**

このアプリには塗りつぶす仕組みが無い（SOLIDも同じ扱い）。
何も出さないと、**そこに何かあることすら分からなくなる**ので、囲いの形は描く。

**(d) 囲いの無いハッチングは、何も出さないし数えもしない。**

ほかの図形にぶら下がっているだけのハッチングがある（実物の図面にあった）。
描くものが無いので、「表示できませんでした」と言うと**無用な心配をかける**（23章）。

**(e) 線が増えすぎないよう、上限を置く。**

壊れた図面で間隔が極端に小さいと、何十万本もの線ができてiPadが止まる。
1つのハッチングにつき4000本までにする。**当たらないのが正常。**

### 38.4 点（POINT）

CADの設定 `$PDMODE` が 0 のとき、点は**小さな丸**として表示される
（実物の図面で確認した）。このアプリでも同じように小さな丸を出す。

大きさは**線の太さに合わせる**。こうすると、画面でも紙でも、
拡大しても縮小しても、ちょうどよい大きさになる。

　　寸法の中に入っている点は、今までどおり出さない（10.5・実物で100個近くあった）。
　　図面に置かれたふつうの点だけを出す。

### 38.5 決まり

- **図形の種類は、むやみに増やさない。** ふつうの線に直せるものは直す。
- 模様や大きさの数字は、**すでに掛け算が済んでいないか**を実物で確かめてから使う。
- 数がいくらでも増えうる計算には、**必ず上限を置く。**

---

## 39章 図面の上で長さを測る（2026-09-04）

### 39.1 指で正確な位置はタップできない ── だから「吸い付き」が要る

第1段階から予定していた「2点をタップして寸法を測る」を作った。

作るうえでいちばん大事なのは、**指では正確な位置をタップできない**という事実である。
1ミリずれれば、測った長さも1ミリ狂う。**それでは現場で使えない。**

そこで、タップした場所の近くにある「きりのよい点」へ**吸い付かせる**
（CADの「オブジェクトスナップ」と同じ考え方）。

| 図形 | 吸い付く先 |
| --- | --- |
| 線 | 両端・真ん中 |
| 折れ線 | すべての角 |
| 円 | 中心・上下左右の4点 |
| 円弧 | 中心・両端 |
| 楕円 | 中心 |
| 点 | その点 |
| 文字 | 吸い付かない（形が無いため） |

実物の図面で確かめた：図面に「400」と書かれている線の両端を、
指のブレのつもりで少しずらしてタップしたところ、**ちょうど 400 mm** と出た。

**近くに何も無ければ、吸い付かせない。** タップした場所をそのまま使い、
「そのまま」と画面に出す。勝手に遠くの点へ引っぱると、そのほうが危ない。

### 39.2 長さの単位は、図面から読む

DXFのヘッダー `$INSUNITS` に、図面がミリなのかメートルなのかが書いてある。
これを読んで `drawing.units` に入れる（4＝ミリ、5＝センチ、6＝メートル…）。

書いていない図面（0＝単位なし）は**ミリとみなす。**
日本の建築・設備の図面はミリで描くのがふつうで、実物の図面も4（ミリ）だった。

　　確かめ方：図面に書かれている寸法値（200・250・400…）と、
　　実際の線の長さが一致するかを見る。実物では完全に一致した。

### 39.3 拡大縮小の作りを、もう一度作らない

「印刷する範囲を囲む」画面は、透明な板をかぶせて指を全部受け取る作りだった。
そのため、32章・33章で**拡大縮小と移動をもう一度作り直すはめになった。**

長さを測る画面では、同じ間違いをしない。**板をかぶせない。**

- 指1本のタップ … `gestures.js` の `onTap` がすでに拾ってくれる。それを回すだけ
- 指2本・ホイール … 今までどおり、図面の拡大縮小・移動がそのまま効く

そのために、測る画面の部品はすべて `pointer-events: none`（指を通す）にする。
ボタンだけが指を受け取る。

**測った印は、図面を動かすたびに置き直す。**
印は画面の座標で置いてあるので、忘れると**印だけが取り残され、
まったく別の場所を測ったように見える。**
描き直しのたびに `refresh()` を呼ぶ。

### 39.4 縮小したまま測ると、狙っていない点に吸い付く

吸い付く範囲は「画面で34ピクセル」で決めている。
これを図面の座標に直すと、**拡大率で割った値**になる。

図面ぜんたいを表示しているとき（拡大率0.11）は、34ピクセル＝図面の309ミリになる。
この広さでは、狙った点ではなく近くの別の点に吸い付いてしまう（実測で確認した）。

**直しようが無いので、代わりに「何に合わせたか」を必ず画面に出す。**

- 1点目のあと … 「1つ目は『線の端』に合わせました」
- 測り終わり … 「合わせた先：端 → 中心」

こうしておけば、おかしければ気づける。
最初の案内にも「細かく測るときは、2本指で拡大してからタップしてください」と書く。

### 39.5 決まり

- **指の位置をそのまま使わない。** 必ず吸い付かせる。ただし近くに無ければ吸い付かせない。
- **吸い付く範囲は、必ず拡大率で割ってから使う。** そうしないと拡大しても細かく狙えない。
- **いちばん近い点を選ぶ。** 「最初に見つけたもの」にすると、
  配管の継ぎ目のように点が集まっているところで必ず狙いを外す。
- **何に合わせたのかを画面に出す。** 測り間違いに気づけるようにする。
- **動いているもの（拡大縮小・移動）を作り直さない。** 板をかぶせない作りにする。

---

## 40章 更新できなくなった端末を、助け出せるようにする（2026-09-07 実機で判明）

### 40.1 何が起きたか

ユーザーの iPad が **v0.3.2 のまま動かなくなった**。
v0.3.3 も v0.3.4 も配信済みで、サーバー側は正しく新しい版を返していた
（31ファイルすべて 200 で配信されていることを実測で確認した）。
仕組みそのものにも問題は無く、きれいなブラウザでは v0.3.2 → v0.3.4 の更新が正常に動いた。

つまり**アプリは壊れていない。端末の中の状態だけが取り残されていた。**

### 40.2 いちばんの問題は「誰にも見えないこと」だった

原因を突き止められなかった。理由は、失敗する場所すべてが黙っていたからである。

| 失敗する場所 | どうなっていたか |
| --- | --- |
| `registration.update()` | `.catch(() => {})` で捨てていた |
| 取り込み（install）の失敗 | Service Worker の中で終わる。画面には何も出ない |
| `main()` の途中での失敗 | `try` が無く、後ろの処理がまるごと飛ぶ |

**アプリはふつうに動くので、ユーザーは「更新が来ない」としか分からない。**
こちらも端末の中を見られない。両側から見えないものは、直しようがない。

> 動かない機能より、**黙って動かない機能**のほうが重い。
> 失敗を握りつぶすコードは、そのぶん将来の自分の目をつぶしている。

### 40.3 いちばん怖いのは「自力で出られない」こと

このアプリはキャッシュ優先で返す（5.1）。オフラインで確実に開くための作りである。
その裏返しとして、**いったん更新に失敗すると、端末側から新しい版を取りに行く手段が無くなる。**

- 再読み込みしても、しまってあるほうが返る
- アドレスに `?v=1` のような印を足しても効かない。
  `caches.match(req, { ignoreSearch: true })` が `?` を無視して照合するため

つまり**閉じ込められる**。実際にユーザーは自力で出られなかった。

### 40.4 逃げ道：一覧に入れないページを1枚置く

`naosu.html` を作った。**`APP_SHELL_FILES` に入れない**のがこのページの全てである。

service-worker.js は「しまっていなければ網の外へ取りに行く」ので、
このページだけは必ず新しいものが届く。
**ここを一覧に足した瞬間、逃げ道は消える。** テストで見張っている。

このページができること：

- 今の状態を出す（サーバーの版／端末の版／オフラインの係／使っている容量）
- 新しい版を取り込む手順をその場で試して、どこで失敗するかを出す
- アプリ本体の置き場だけを空にして、取り直させる

守っていること：

- **IndexedDB（図面の実データ）には一行も触れない。** Cache Storage とは別の保管場所なので、
  図面が消えることは構造上ありえない（5.4）。
  現場で図面が消えるのは、更新できないことよりずっと重い事故である。
- DWGを読む部品（約10MB）は消さない。消すと10MBの読み直しになる（35.3）。
- 中身は1ファイルに全部書く。外のJSやCSSを読むと、
  **それが「しまってある古いもの」に化ける**。逃げ道に、逃げたい相手が混ざる。

### 40.5 自分で書いた落とし穴を、自分で踏んだ

診断で「サーバーの版」を `src/version.js` から読もうとして、
`?naosu=…` を足せば新しいものが取れると思っていた。取れなかった。
**40.3 に自分で書いた `ignoreSearch: true` に、そのまま引っかかった。**
サーバーは v0.3.5 なのに、画面には v0.3.2 と出ていた。

網の外に本当に出られる道は、実測で2つ確かめた。

| 道 | 結果 | なぜ |
| --- | --- | --- |
| `GET ./src/version.js?x=…` | 古い写しが返る | 一覧に入っている＋`?`は無視される |
| `GET ./service-worker.js?x=…` | **新しいものが返る** | 一覧に入っていない |
| `HEAD ./src/version.js` | **網の外に出る** | `req.method !== 'GET'` で素通しされる |

そこで、サーバーの版は `service-worker.js` の `CACHE_VERSION` から読む
（`APP_VERSION` と一致することはテストで保証されている。3.2）。
通信できているかの確認は `HEAD` で行う。

> **書いた注意書きは、自分にも効く。** 落とし穴を文章で理解していても、踏むときは踏む。
> 「たぶん大丈夫」で済ませず、実際に取れた値を目で見て確かめること。

### 40.6 二度と黙って止まらせない

- **更新の見張りは、`main()` のいちばん最初に始める。**
  以前は最後にあった。手前の処理（図面の読み直しなど）が1つでも失敗すると、
  **更新の見張りが永久に始まらない。** アプリは動くので誰も気づけない。
  更新の道だけは、何があっても必ず通す。
- **`update()` の失敗を捨てない。** ただし通信が無いだけなら失敗ではない（オフラインで使うアプリ）。
  `navigator.onLine` のときだけ知らせる。
- **取り込み失敗（`redundant`）を拾う。**
  ただし `redundant` はふつうの世代交代でも起きる。
  一度も `installed` に届かなかったときだけ「失敗」と呼ぶ。
- **見に行くたびに、待機中の新しい版を拾い直す。**
  `updatefound` の合図だけに頼ると、**一度取りこぼしたら二度と案内が出ない**。
  すでに待機中のSWには、もう合図が出ないためである。
- 失敗したら帯を赤くして「直しかた」（naosu.html）への行き先を出す。
  お知らせと同じ青のままでは見過ごす。

### 40.7 覚えておくこと

- **オフライン対応は、閉じ込めと表裏一体である。** 入れ物を作ったら、必ず出口も作る。
- **失敗は、必ず人が見えるところまで持ち上げる。** 握りつぶした失敗は、原因調査そのものを不可能にする。
- **いちばん大事な処理を、いちばん後ろに置かない。** 手前が倒れたら道連れになる。
- `display` を指定した要素は、`hidden` を付けても消えない。
  ブラウザ既定の `[hidden]{display:none}` を打ち消すため、`[hidden]` の指定を別に書く。

---

## 41章 実機で分かった4つのこと（2026-09-07 ユーザーの指摘）

ユーザーがiPadで使って、まとめて4つ挙げてくれた。ここに残す。

### 41.1 「終わる」ボタンが効かない ― 同じ名前が世の中にある

押しても反応がない。**エラーも出ない。** いちばん見つけにくい壊れ方だった。

原因は measure-ui.js の中のこの行である。

```js
root.querySelector('.ms-close').addEventListener('click', () => stop());
```

この `stop` は、返り値のオブジェクトに入れたほうの `stop` を呼ぶつもりで書いていた。
しかし**そこからは見えない**。見えないなら本来 ReferenceError で落ちる。
ところがブラウザには `window.stop()`（ページの読み込みを止めるもの）が最初からある。
そのため**エラーも出ず、静かに何も起きなかった。**

> 「呼んでいるのにエラーが出ない」は、直っている証拠ではない。
> **世の中に同じ名前のものがあるだけかもしれない。**

危ないのは `stop` だけではない。`close` `open` `print` `focus` `blur`
`find` `scroll` `alert` も、すべて `window` にある。
どれも「自前の関数のつもり」で書きやすい名前である。

対策：**そのファイルの中で呼ぶものは、そのファイルの中で定義する。**
機械にも見張らせている（tests/measure.test.js）。裸で呼んでいる名前が
ファイルの中で定義されていなければ、テストが落ちる。

### 41.2 抜ける道は、いつでも見えているところに置く

「終わる」ボタンは、**2点そろうまで画面に無かった**。
結果の箱ごと `hidden` にしていたためである。
測りかけでやめたいとき、抜ける道が画面のどこにも無かった。

> 作業の途中でやめたくなるのは、**うまくいっていないとき**である。
> そのときにこそ、抜ける道が見えていなければならない。

### 41.3 寸法線に引っ張られる

「吸い付きはさほどよくない。寸法線の方に引っ張られる場合がある。」

寸法線は、測りたい線の**すぐ横に平行に**引かれ、補助線の端は
測っている線の端と**同じ座標にある**。狙った線をタップしたつもりで寸法線に吸い付く。

外してしまうのは違う。「この寸法線の端から端まで」を測りたいこともある。
そこで**後回しにする**ことにした。寸法線の点は、本物の線より
3倍ちかくないと勝てない（`DIM_PENALTY`）。
本物が近くにあれば本物が勝ち、寸法線しか無ければ寸法線に吸い付く。

寸法から作った図形には、展開の前後で図形の数を比べて印を付ける
（作る場所が何か所もあるので、1か所ずつ足すより確実）。
吸い付いた先は「寸法の端」のように見せて、気づけるようにする。

### 41.4 1点だけ直せるようにする

「修正ができないので何回もやり直しになります。」

2点目だけ外したときに、合っている1点目まで捨てさせていた。
「1つ前にもどる」を足して、1点だけ戻せるようにした。

> やり直しの単位は、**間違えた単位に合わせる。**
> 1つ間違えたら1つだけ戻せること。

### 41.5 「PDFで保存」の場所が分からない

「pdfで保存これはどこのボタンから操作しますかも場所がわかりません」

置いてあったのは、範囲を囲んだあとの確認画面である。
実測では iPad の縦でも横でも、ちゃんと画面に出ていた（隠れてはいない）。
**隠れていなくても、そこにあると分からなければ無いのと同じ。**

手前のボタンの名前を「この範囲を印刷」→「この範囲を印刷／PDFで保存」に変えた。
押す前に、その先に何があるかが分かる。

### 41.6 印刷しないレイヤーを、紙に出さない

「このキャドの図面で印刷しない設定になっているものを読み取ることはできますか。
印刷しない線もこちらでは表示されるようになっているので印刷もそのまま印刷されます。」

CADには「画面には出すが、紙には出さない」レイヤーがある。作図の補助線などが置かれる。
見分け方は2つあり、どちらもCADの決まりである。

| 見分け方 | 意味 |
| --- | --- |
| コード290 が 0 | このレイヤーは印刷しない |
| 名前が DEFPOINTS | AutoCADが作る特別なレイヤー。必ず印刷されない |

**画面には出す。紙には出さない。** CADがそう振る舞うからである。
消してしまうと画面からも無くなり、CADと見た目が変わってしまう。

外すのは紙に出す道だけ（`printableDrawing`）。
**絵（PNG）とPDFの両方で外すこと。** 片方だけ直すと、
確認画面では消えているのに紙にだけ出るという、いちばんたちの悪いずれ方になる（36.2）。

黙って外さない。確認画面に「印刷しない設定の線 N本は、紙には出しません」と出す。
画面には見えているので、何も言わないと「消えた」と誤解される。

### 41.7 覚えておくこと

- **エラーが出ないことは、動いている証拠にならない。** 実際に押して、結果を目で見る。
- **抜ける道は、いつでも見えているところに置く。**
- **やり直しの単位を、間違えた単位に合わせる。**
- **隠れていなくても、分からなければ無いのと同じ。** 押す前に、その先が分かる名前にする。
- **画面と紙は、意図して違ってよい。** ただし、違うことは必ず伝える。

---

## 42章 印は「戻す」のではなく「動かす」（2026-09-07 ユーザーの指摘）

### 42.1 言われたこと

> 長さ計測でひとつ前に戻るではなく、そのポインターを移動できるようにしたい

41.4で足した「1つ前にもどる」は、**消してから置き直す**やり方だった。
1回で済むところが2回になる。ずれているのは位置だけなのだから、位置だけ直せればよい。

> **やり直しは「消して作り直す」より「今あるものを直す」ほうがよい。**
> 消すと、正しかった部分まで一度失われる。

「1つ前にもどる」は外した。ボタンは「はじめから」「終わる」の2つになった。

### 42.2 動かしている間も、吸い付かせる

指を置いた場所をそのまま使うと、動かすたびに1ミリ単位でずれる。
それでは動かせる意味がない。**タップのときとまったく同じ道を通す。**

そのため吸い付きの計算（画面の位置 → きりのよい点）を `app.js` の `snapAt` に1つだけ置き、
タップからも、つまんで動かすときからも、そこを呼ぶ。
2か所に書くと、片方だけ直したときに
「タップでは吸い付くのに、動かすと吸い付かない」というちぐはぐが起きる。

### 42.3 丸だけが指を受け取る。2本目が来たら譲る

この画面は板をかぶせず、指を canvas に素通しさせている（39章）。
丸をつまめるようにするには、**丸だけ**が指を受け取ればよい。

ただし、丸の上で2本指を始められると拡大縮小が効かなくなる。
そこで**2本目の指が来たら、つまむのをやめて譲る**。
32章・33章で作った拡大縮小を、ここでもう一度作らないための決まりでもある。

`window` に付けた見張りは、**画面を閉じるときに必ず外す。**
外し忘れると、閉じたあとも残り続ける。

### 42.4 `inset` で当たり判定を広げてはいけない

丸の見た目は16px。指で狙うには足りないので、当たり判定だけを44pxに広げた。
最初はこう書いた。

```css
.ms-dot::before { inset: -14px; }   /* 16 + 14×2 = 44px のつもり */
```

**実測したら38pxだった。**

このアプリは全体に `box-sizing: border-box` を掛けている（ui.css）。
そのため `width: 16px` には枠線3px×2が含まれ、中身は10pxしかない。
`inset` は中身からの距離なので、10 + 28 = 38px にしかならない。

```css
.ms-dot::before { width: 44px; height: 44px; }  /* 大きさを直接書く */
```

> **「計算すれば44pxになるはず」は、確かめたことにならない。**
> 実際に画面で測る。ここでも、測らなければ6px足りないまま出していた。

### 42.5 画面の向きが変わったとき、イベントを当てにしない

測る画面と、範囲を囲む画面は、どちらもキャンバスに位置と大きさを合わせている。
合わせ直す合図は `resize` と `orientationchange` だけだった。

**これでは取りこぼす。** 実測で、幅を768pxにしたあとも枠が1280pxのまま残った。
合図が届いた時点で、キャンバスがまだ古い大きさだったためである。
結果、**「終わる」ボタンの右端が846px（画面は768px）で、画面の外へ出た。**
現場でiPadを回したら、抜けるボタンが押せなくなる。

そこで、合図を待つのをやめて **`ResizeObserver` でキャンバスの大きさを直接見張る。**
app.js の `watchLayoutChanges`（30章）と同じ考え方である。
両方の画面に入れた。

> **「起きるはずのイベント」に頼らない。知りたいのは大きさなのだから、大きさを見張る。**

### 42.6 覚えておくこと

- **直せるものは、消さずに直す。**
- **同じことをする道は1本にする。** 2本あると、片方だけ直したときにちぐはぐになる。
- **計算で出した寸法は、画面で測って確かめる。** box-sizing ひとつで6px変わる。
- **イベントではなく、知りたいものそのものを見張る。**

---

## 43章 図面に文字を書き足す（2026-09-07）

第1段階から予定していた最後の機能。作る前に、決めるべきことをユーザーに聞いた。

| 聞いたこと | 答え |
| --- | --- |
| 書いた文字を覚えておくか | **残す** |
| 紙に出すか | **印刷とPDFの両方に入れる** |
| あとで直せるか | **すべて（動かす・書き直す・消す）** |

> **作り方が分かれる問いは、作る前に聞く。**
> あとで聞くと、作り直しになる。

### 43.1 新しい種類を増やさず、すでに通っている道に乗せる

注記は「文字（text）」として持つ。新しい図形の種類は作らない。

このアプリは text をすでに3か所で描ける。**画面・印刷の絵・PDF** である。
新しい種類にすると、その3つすべてに描き方を足すことになり、
どれか1つを忘れれば「画面には出るのに紙に出ない」という食い違いが起きる。
36.2で実際に痛い目を見ている。

ただし `isNote` の印を必ず付ける。印が無いと、
覚えるときに**図面のもとの文字と見分けが付かなくなる**。

図面のファイル自体には書き戻さない。**お客様のファイルは書き換えない。**
端末の中に、図面と別に覚えておく。

### 43.2 文字の大きさは「置いたときに見えている大きさ」で決める

図面の単位（ミリ）で決め打ちすると、図面の縮尺しだいで
極端に大きくなったり、点のように小さくなったりする。

置いたときの拡大率で割って、**画面で20pxに見える大きさ**にする。
置いた本人の感覚と合う。

ただし、これは万能ではない。
図面ぜんたいを表示したまま置くと、拡大したときに文字が大きすぎる。
逆も起きる。**置きたい大きさで見ているときに置く**のがよい。

### 43.3 書いたらすぐ覚える

現場では、書いた直後にアプリを閉じることがふつうにある。
「あとでまとめて保存」にすると、そこで消える。
足す・動かす・書き直す・消すの**どれをしても、その場で覚え直す**。
覚えるのは注記だけなので、図面が大きくても軽い。

**同じ図面をもう一度開いたときに、書いた文字を巻き添えにしない。**
`saveDrawing` は記録を丸ごと置き換えるので、
そのまま書くと覚えてあった注記が黙って消える。前の記録から引き継ぐ。

**文字を書いただけでは、図面の「新しい順」を変えない。**
変えると、次に開いたときに別の図面が出てきて驚かせる。

**あとから届いた注記で、取り違えない。**
大きな図面だと、覚えてある文字を取り出している間に
ユーザーが別の図面へ切り替えているかもしれない。
戻す直前に、今開いている図面かどうかをもう一度確かめる。
省くと、**別の図面に他の図面の文字が現れる。**

### 43.4 画面の作りは、測る画面をそのまま踏襲する

板をかぶせない。指は canvas に素通しさせ、拡大縮小・移動は今までどおり効かせる。
受け取るのは**ボタンと、注記の取っ手だけ**。
取っ手の上で2本指を始められたら、つまむのをやめて譲る（42.3）。
当たり判定は `inset` ではなく大きさを直接書く（42.4）。
画面の向きは `ResizeObserver` で見張る（42.5）。

図面を動かしたら取っ手も置き直す。忘れると取っ手だけ取り残される（39.3）。

### 43.5 入力欄の文字は16px以上にする

iPadのSafariは、**小さい文字の入力欄に触れると画面ぜんたいを勝手に拡大する。**
図面を見ている位置がずれて驚かせる。17pxにした。

### 43.6 「ゆるいテスト」をもう一度やってしまった

「図面を覚え直しても、書いた文字は消えない」ことを、
`前の記録` という字がコードにあるかで見ていた。
値を `notes: []` に書き換えても**通ってしまった**（わざと壊す確認で判明）。

```js
// だめ：字があるかを見ているだけ
assert.match(body, /前の記録/);
// よい：前の記録の中身を入れているところまで見る
assert.match(body, /notes:\s*\(?\s*前の記録/);
```

41章でも同じ形の見落としをしている。

> **「その名前が出てくるか」で確かめない。「そう使われているか」で確かめる。**

### 43.7 覚えておくこと

- **作り方が分かれる問いは、作る前に聞く。**
- **新しい種類を増やす前に、すでに通っている道に乗せられないか考える。**
  道が1本なら、忘れる場所も1つで済む。
- **お客様のファイルは書き換えない。**
- **書いたらすぐ覚える。** 現場は、いつ閉じられるか分からない。
- **あとから届いたものは、届け先が変わっていないか確かめてから渡す。**

---

## 44章 書いた文字に、色・大きさ・向き・改行を足す（2026-09-07 ユーザーの指示）

> 文字の色を選択できるように（数は多くなくて結構）／文字の大きさも選択できるように／
> テキストを15度間隔で回転できるように／改行できるように

### 44.1 改行のために、本体と図形を分けた

いちばん作りが変わったのは**改行**である。
1つの注記が2行なら、描くための文字（text）は2つ要る。

そこで持ち方を変えた。

| | 前（43章） | 後（44章） |
| --- | --- | --- |
| 本体 | drawing.entities の text そのもの | **drawing.notes**（色・大きさ・向き・文字） |
| 描くもの | 同上 | notes から**作り直す**（1行につき1つ） |

図形の側を本体にすると、1つの注記が図形2つに散らばり、
動かす・消すのたびに「どれとどれが仲間か」を数えることになる。
本体を1つにしておけば、散らばるのは作り直すときだけで済む。

**1行につき1つの図形**にしたので、render.js も print-area.js も print-pdf.js も
ふつうの1行の文字として扱うだけでよい。**改行のために3か所を直さずに済んだ。**

回転も、調べたら3か所とも**すでに対応していた**（PDFは漢字の絵型まで回せていた）。
43.1で「すでに通っている道に乗せる」と決めたことが、そのまま効いた。

> **持ち方を1つに決めておくと、機能が増えても散らからない。**

作り直しでは、消す相手を `isNote` の印だけで選ぶ。
印を見ずに「レイヤー名で消す」などにすると、**図面のもとの文字まで消える。**

行が下がる向きは、注記の向きに合わせて回す。
回さないと、傾けた注記の2行目が明後日の方向へ出る。

### 44.2 色と大きさ

- 色は5つ（赤・黒・青・緑・だいだい）。ユーザーの指示で多くしない。
- **薄い色を入れない。** 現場のプリンターは白黒のことがある。
  灰色に変わったときに読めない明るさの色は入れない（テストで見張っている）。
- 大きさは3段階（小・中・大）。数字は「置いたときに画面で何ピクセルに見えるか」（43.2）。

**大きさは、触ったときだけ計算し直す。**
触っていないのに計算し直すと、**文字を直しただけで大きさが変わる**。
置いたときと書き直すときとで、拡大率が違うためである。

### 44.3 改行できるようにしたので、Enterは改行に返す

43章では Enter で決定していた。改行を入れられるようにした以上、
**Enter は改行のためのもの**である。両方に使うと、どちらも思いどおりにならない。
決定は「決定」ボタンだけで行う。

入力欄も `<input>` から `<textarea>` に変えた。

### 44.4 すでに文字があるところをタップしたら、重ねずに書き直す

重ねると、どちらも読めなくなる。
タップした場所に注記があれば、新しく置かずに書き直しの窓を出す。

当たり判定は、回す前の四角で見る。
回した四角で正確に見てもよいが、**指の誤差のほうがずっと大きい**ので、
手間に見合わない。まわりに余裕を足したほうが実用的である。

### 44.5 選ぶ段を足したら、窓が高くなった

色・大きさ・向きの3段を足したぶん、窓が432pxになった。
1024×768 の画面には収まる。**しかしキーボードが出ると見える範囲が半分ほどになる。**
そのままでは「決定」が画面の外へ出て、書いたものを確定できない。

窓の中でスクロールできるようにした（`max-height` と `overflow-y: auto`）。

> **入れ物に物を足したら、入れ物の外側（キーボードが出た状態）も見る。**

### 44.6 覚えておくこと

- **機能が増える前提で、持ち方を1つに決めておく。**
  本体を1つにしておけば、増えても散らからない。
- **同じキーを2つの意味に使わない。** 改行にするなら、決定はボタンに返す。
- **すでにあるものの上に、黙って重ねない。**
- **古い記録が開けなくなっていないか、必ず確かめる。**
  色も向きも無かった v0.3.8 の注記が、v0.3.9 でそのまま開けること（テストあり）。

---

## 45章 自由曲線（SPLINE）に対応する（2026-09-08 ユーザーの依頼）

> 「自由曲線これは出てくると思いますので読み込めるようにしてください」

### 45.1 実物の図面を先に調べた ── 「出てくると思う」は当たっていた

作る前に、お客様の `参考図.dxf` を数えた。

| 分かったこと | 数 |
| :--- | :--- |
| ファイルの中にある SPLINE | **2個** |
| そのうち画面に出ていたもの | **0個** |

**「表示できませんでした」には1個も出ていなかった。** なぜか。

2個とも「**ベント**」という部品（ブロック）の中に入っていて、
その部品はこの図面では**一度も配置されていなかった**からである。
（さらにそのうち1個は「見えない」指定つきの、動的ブロックの控えだった）

> **つまり、今は困っていないが、その部品を1回置いた瞬間に、
> 何の断りもなく曲線が消える図面ができあがる。**

お客様の「出てくると思います」は、この意味で正しかった。
**実物にあった曲線で確かめられたので、机上の想像で作らずに済んだ。**

### 45.2 やり方：折れ線に直して、すでに通っている道に乗せる

**新しい図形の種類は増やさない。** ハッチング（38章）・注記（43章）と同じ形で解決した。

自由曲線（NURBS＝ナーブス）の式を読み取り、**細かい直線をつないだ折れ線（polyline）**に直す。
折れ線はこのアプリがすでに3か所（画面・印刷の絵・PDF）で描けるので、
どこにも描き方を足さなくてよい。

　　種類を増やすと、3か所すべてに描き方を足すことになり、
　　1つ忘れれば「画面には出るのに紙に出ない」が起きる（36.2・43.1）。

計算は `src/spline.js` に置いた。画面に触らない「数を入れて、数が返る」だけの係なので、
そのまま試験できる。DXFの読み取りは `dxf-parse.js` の `convertSpline()` が受け持つ。

### 45.3 気をつけたこと

**(a) 重み（コード41）を読み落とすと、形が変わる。**

自由曲線には「重み付き（有理）」という書き方がある。磁石の強さのようなもので、
これを使うと円弧や楕円弧を**ぴったり**表せる。
重みを無視すると、同じ制御点でも別の形になってしまう。

**(b) 閉じた曲線には、書き方が2通りある。**

| 書き方 | 中身 | 気をつけること |
| :--- | :--- | :--- |
| クランプ式 | 制御点の先頭が末尾にも書いてある | そのまま描けば閉じる |
| 周期式 | 制御点は**1周ぶんだけ** | 自分で1周させないと**輪が欠けて出る** |

見分けは「両端のノット（目盛り）が重なっているか」で行う。
**見分けを間違えると、どちらの向きにも壊れる**（閉じないか、二重になるか）。

**(c) ノット（目盛り）が書かれていない図面が、実際にある。**

数が合わない・1つも無いDXFがある。そのときは両端で止まる目盛りを自分で作る。
**これを捨ててしまうと、描けるはずの曲線が消える。**

**(d) 点の数に上限を置く（MAX_SPLINE_POINTS＝400）。**

実務図面は数万本の線がある。細かく分けすぎるとiPadが固まる。
`hatch.js` の `MAX_HATCH_LINES` と同じ考えの安全装置で、**当たらないのが正常。**
実物の曲線は65点だった（上限の6分の1）。

　　区間が多すぎて「1区間1本」でも収まらない曲線は、区間を無視して
　　端から端まで等間隔に割る。**上限を超えるくらいなら、粗くなるほうがよい。**

**(e) それでも形にならないものは、黙って捨てずに数える（10.5）。**

制御点も通過点も無い壊れたSPLINEは `SPLINE（形が読み取れない）` として数える。
一方、**対応したので `SPLINE` を数えるのはやめた**（18.4）。

### 45.4 「ゆるいテスト」を、今度は先に見つけた

わざと14通りに壊して、テストが落ちるか全部確かめた（6.3）。
**1つだけ、壊しても通ってしまうものがあった。**

「クランプ式の閉じた曲線を、誤ってもう一度1周させる」壊し方である。
テストは「点が制御点の四角からはみ出していないか」を見ていたが、
**間違って1周させても、点はぜんぶ四角の中に収まる**（曲線の性質）ので気づけなかった。

クランプ式は**最初の制御点をきっちり通る**ので、そこを見るように直したら落ちるようになった。

> **「それらしい範囲に入っているか」で確かめない。
> 「そうでなければ必ず変わる1点」で確かめる。**

43.6・41章と同じ形の見落としで、これで3回目。

### 45.5 数字で押さえられる見本を作った

見本は「重み cos45度・制御点3つ・次数2」にした。これは**数学的にぴったり四分円**になるので、
「それらしい曲線が出た」ではなく「中心からの距離が100か」を1点ずつ検算できる。
実測のずれは 1.4×10⁻¹⁴ だった。

> **正解が式で分かる形を見本に選ぶと、テストが「見た目」から「検算」になる。**

### 45.6 目で見て確かめたこと（8.2）

テストが通っただけでは終わりにせず、実物を描かせて見た。

- お客様の `参考図.dxf` の「ベント」ブロックを配置したDXFを作り、画面に描かせた
  → 通気管の記号が、なめらかな曲線として出た（折れ線65点）
- 同じものを**印刷の絵**と**PDF**にも出した
  → PDF 1.9KB・紙に出た図形6個（＝画面の図形数と一致）。画面と紙で食い違わない（36.2）

### 45.7 まだ対応していないもの

`3DFACE` `MLINE` は今までどおり数えるだけ。今回は手を付けていない。

### 45.8 覚えておくこと

- **作る前に、実物の図面を数える。** 「あるはず」で作らない。今回は2個あった。
- **新しい種類を増やす前に、すでに通っている道に乗せられないか考える。**
  道が1本なら、忘れる場所も1つで済む。これで3件目（38章・43章・45章）。
- **数がいくらでも増えうる計算には、必ず上限を置く。**
- **正解が式で分かる見本を選ぶ。** 検算できるテストだけが、何かを守っている。
- **わざと壊して、落ちることを確かめる。** 今回も1つ、ゆるいテストが見つかった。

---

## 46章 属性の文字と引出線に対応する（2026-09-08 ユーザーの報告）

ユーザーが自分の図面を v0.4.0 で開いて、こう出た。

> この図面のうち 144個の図形は表示できませんでした
> （種類：ATTDEF 66個、ATTRIB 66個、MULTILEADER 12個）

### 46.1 3種類のうち、1種類は「数え方の間違い」だった

| 種類 | 何か | 図面から欠けていたか |
| :--- | :--- | :--- |
| **ATTDEF** 66個 | 部品の中の「文字の枠」の**定義（型）** | **欠けていない。数えたのが間違い** |
| **ATTRIB** 66個 | その枠に**実際に入っている文字**（機器番号・呼び径） | **欠けていた** |
| **MULTILEADER** 12個 | **引出線**（矢印＋線＋文字） | **欠けていた** |

ATTDEFとATTRIBが同じ66個だったのが手がかりになった。
部品の中に66個の「枠」があり、それを置いたときに66個の「実際の値」が入る、という関係である。

> **数が一致しているときは、たいてい親子の関係がある。**
> どちらか一方だけを直すと、もう一方が浮く。

### 46.2 ATTDEFを数えてはいけない理由

ATTDEFは「ここに機器番号が入ります」という**型**であって、図面の線ではない。
部品を置いたときに画面へ出るのは、型ではなくATTRIB（実際の値）のほうである。

**つまり、型そのものは図面から欠けていない。**
これを数えると、無用な心配をかけ、**本当に足りない図形が埋もれる**（18.1）。
実際、この図面では「144個」のうち66個が、この空振りだった。

ただし、次の2つだけは本当に画面に出るので描く。

- **「一定」の印が付いた型**（コード70の2のくらい）
  … 値が決め打ちで、ATTRIBが作られない。**型が飛ばされると、この文字だけ消える**
- **部品の中ではなく、図面に直接置かれた型**
  … CADは名札（タグ）を表示する

### 46.3 位置ぞろえがあるときは、2つめの点に置く

ATTRIBには位置ぞろえ（コード72・74）がある。
**ぞろえの指定があるときは、1つめの点(10,20)ではなく2つめの点(11,21)が実際の置き場所になる。**

属性の文字は、丸の中の番号のように中央ぞろえで置かれることが多い。
ここを取り違えると、**番号が丸から外れて出る。**

### 46.4 引出線は、入れ子になっている

MULTILEADERの中身は、かっこで囲まれた入れ子である。

```
CONTEXT_DATA{        文字と、その置き場所
  LEADER{            引出線1本ぶん
    LEADER_LINE{ }   折れ線の頂点
  }
}
```

**【落とし穴】コード304は2つの意味を持つ。**
「文字の中身」でもあり、「LEADER_LINE{」という区切りでもある。
番号だけで見分けると、**区切りの記号が文字として図面に出る。**

そこで 300〜305 のうち

- 値が `{` で終わる → 入れ子の始まり
- 値が `}` → 入れ子の終わり
- それ以外 → 中身（304なら文字）

として扱う。**閉じかっこの番号を当てにしない**ので、書き出すソフトの差にも強い。

> **番号ではなく、値を見て決める。** 同じ番号が2つの意味を持つ形式は珍しくない。

描き方は今までどおり。引出線は**折れ線**、文字は**文字**。新しい種類は増やさない（4件目）。
矢印の頭（黒い三角）は出さない。塗りつぶす仕組みが無いためで、
線がどこを指しているかは折れ線だけで分かる（38.3(c)と同じ）。

### 46.5 実物で分かった：LEADER_LINE には頂点が1つしか入っていない

公開後、ユーザーから報告があった。

> 「引出線これは文字正常に表示されていますが指定された矢印の方向が全然違います」
> 「矢印線がアンダーバーのようになっている」

**この一言で場所が決まった。**
引出線は「折れ曲がりの短い線（ランディング）」と「引出しの線本体」の2つでできている。
短い横線だけが出ているということは、**LEADERは読めていて、LEADER_LINEだけ読めていない。**

> **症状を「どこが出て、どこが出ていないか」に分けて聞くと、
> コードを読む前に場所が決まる。**

実物のDWGを変換して中身を見たら、こうなっていた。

```
302 LEADER{
  10 30126.77      ← 引出線の「終わりの点」は、ここにある
  11 1.0           折れ曲がってから伸びる向き
  40 15.0          その長さ
  304 LEADER_LINE{
    10 29977.05    ← 頂点は、この1つだけ（矢印の先端）
  305 }
303 }
```

**LEADER_LINE には、矢印の先など「途中までの点」しか入っていない。**
線の終わりの点は、ひとつ外側の LEADER が持っている。

こちらは「2点以上でなければ線として捨てる」としていたため、
点1つの線が丸ごと捨てられ、折れ曲がりの短い線だけが残っていた。

> **「点が足りないから捨てる」の前に、「足りない点はどこかに無いか」を見る。**

### 46.6 矢印の頭は「くの字」の2本の線で描く

はじめは矢印の頭を描いていなかった（塗りつぶす仕組みが無いため）。
だが**どちらを指しているのか分からない注記は、現場で役に立たない。**

黒い三角は作れないが、**開いた矢印（くの字）なら線2本で描ける。**
いつもの道（line）だけで済み、新しい種類は増やさない。

- 向き … 点の並びの**先頭が矢印の先**。そこから2番目の点へ向かう向きに、左右15度ずつ開く
- 大きさ … コード140（実物では25で、文字の高さと同じだった）
- **大きさが書かれていなければ、頭を作らない。** 勝手な大きさで描くと図面によって不釣り合いになる

### 46.7 確かめられたこと（実物のDWGで）

| 見たこと | 結果 |
| :--- | :--- |
| 図形の数 | 69,475個 |
| 表示できませんでした | **0個** |
| 引出線 | 16本すべて、矢印の先から文字まで線が通った |
| 文字の高さ | コード41をそのまま使う（コード40の拡大率を重ねて掛けない）で正しかった |
| 文字の位置 | コード12・22、左ぞろえ・高さまん中で正しかった |

46.5で「掛けるべきか分からない」と書いていた拡大率は、**掛けないのが正しかった。**
ハッチング（38.3(a)）と同じ結論で、迷ったら掛けないほうへ倒すのがよい。

### 46.8 覚えておくこと

- **数えてよいものと、いけないものを毎回考える。**
  「表示できませんでした」は、図面が欠けていることを知らせるためのもの。
  欠けていないものを数えると、案内そのものが信用されなくなる（18章）。
- **数が一致しているときは、親子の関係を疑う。**
- **同じ番号が2つの意味を持つことがある。番号ではなく値を見る。**
- **手元に無い図面のために作ったものは、「確かめていない」と書いて残す。**
  黙っていると、次の人が確かめ済みだと思ってしまう。今回はそれが効いて、
  ユーザーが実物で見てくれたおかげで残りの不具合が出た。
- **症状は「どこが出て、どこが出ていないか」に分けて聞く。**
  「アンダーバーのようになっている」の一言で、直す場所が決まった。
- **「点が足りないから捨てる」の前に、「足りない点はどこかに無いか」を見る。**
- **迷ったら、拡大率は掛けないほうへ倒す。** ハッチング（38.3(a)）でも引出線でも、
  数字はすでに掛け算が済んでいた。

---

## 47章 鏡像（裏返し）で置いた部品を、正しく出す（2026-09-08 ユーザーの報告）

> 「DWGのブロックの表示が反転しているところがちらほらあります。
> 私の予想ですがブロックを鏡像しているところかと思います」

**予想は当たっていた。** 調べたところ、鏡像で置いた部品の
**位置は合っているのに、円弧のふくらむ向きと文字の向きだけが裏返っていなかった。**

### 47.1 位置だけ合っていると、いちばん気づきにくい

置き場所は行列で計算していたので、鏡像でも正しかった。
ところが**角度だけ、行列とは別に「足し算」で持ち回っていた。**

```js
// 悪い：回転の角度を、行列とは別に足し算で持ち回っていた
rotationDeg: ctx.rotationDeg + rotationDeg,
```

足し算では「裏返し」を表せない。だから鏡像が混ざったとたんに合わなくなる。
しかも**位置は合っている**ので、図面はそれらしく見えてしまう。
「配管のエルボ（曲がり）が反対向き」という、**形だけがおかしい**壊れ方になる。

> **同じことを2通りの持ち方で持たない。**
> 持ち方が2つあると、必ずどちらかが取り残される。36.2と同じ話。

直し方：**角度は行列から取り出す**（`transformAngle`）。持ち方を1つにした。
これで、回転も鏡像も入れ子も、勝手に正しくなる。

### 47.2 裏返すと、回る向きが逆になる

行列式（`a*d - b*c`）がマイナスなら裏返っている（`isMirrored`）。
裏返ると**反時計回りが時計回りになる**ので、円弧の始まりと終わりを**入れ替える**。

入れ替えを忘れると、円弧が「反対側の残りの部分」として描かれる。
右上へふくらむはずのものが、**残り270度のほうになってしまう。**

楕円も同じだが、楕円の角度は数え方が違う（つぶす前の円で測る）ので、
180から引くのではなく**符号を反転して入れ替える**。

### 47.3 鏡像の置き方は2通りある。どちらも行列に入れる

| 書かれ方 | 意味 |
| :--- | :--- |
| 拡大率がマイナス（41か42が -1） | CADで「鏡像」したときのふつうの書かれ方 |
| 押し出しZが -1 | 裏から見た置き方 |

**押し出しZが-1の部品は、置く点と向きだけ直しても直らない。**
以前は「置く点のXをマイナスにして、向きを180から引く」としていたが、
これは**180度回転**であって鏡像ではない。上下がひっくり返る。

正しくは、部品を置いた結果を**まるごと左右に裏返す**。
行列の最後に左右反転を掛ければよい。

### 47.4 同じ反転を2回かけて、打ち消し合っていた

楕円で、軸の向きを2か所で反転させていた。

```js
majorX = flipXIf(flip, ...)                    // ここで1回
rotationDeg = Math.atan2(majorY, majorX)       // もう反転済みの値から角度を出している
if (flip) rotationDeg = 180 - rotationDeg;     // ここでもう1回 ← 余計
```

反転が2回かかって元に戻り、**反転していないのと同じ**になっていた。

> **反転は「どこで1回かけるか」を決めて、そこ以外では触らない。**
> 2回かかると、テストも通ってしまう（元に戻るので、ふつうの図面では正しい）。

### 47.5 文字だけは、裏返さない

線や円弧は裏返してよいが、**文字を裏返すと読めなくなる。**
CADも、鏡像にした文字を裏返さずに読めるまま出す決まりになっている（MIRRTEXT）。

このアプリには「裏返した文字」を描く仕組みが無いので、
向きだけ素直に裏返すと**水平の文字が上下さかさま**になる。
さかさまの文字は、現場でいちばん困る。

そこで、**裏返しの中にある文字だけ**、読める側へ倒す。
**裏返していない図面の文字には、いっさい手を触れない。**
図面がもともとさかさまに書いている文字を勝手に直すと、CADで見た図面と食い違う。

### 47.6 「水平の文字」だけで試すと、テストが何も守らない

はじめ、鏡像の見本に水平の文字（傾き0度）しか入れていなかった。
ところが水平の文字は、**読める向きへ倒す処理が有っても無くても同じ結果**になる。
わざと壊す確認で、この見落としが出た。

45度に傾いた文字を足したら、落ちるようになった。

> **裏返しても同じに見えるものを、見本にしない。**
> 円・正方形・水平の文字は、間違っていても気づけない。

同じ理由で、見本の部品は
「右上へふくらむ円弧」「上へ伸びる縦線」「傾いた文字」だけにしてある。

### 47.7 三角関数を通すと、きれいな数でなくなる

角度を足し算から行列に変えたら、`30` が `29.999999999999993` になった。
図面の精度からすれば無い差だが、読みにくく、比べにくい。
10億分の1度で丸めて、きれいな数に戻している。

実物の参考図で直す前と比べたところ、**変わったのは丸めだけ**で
（`315.00000000000074` → `315`）、図形の数も中身も同じだった。

### 47.8 手元に無いものを直し、ユーザーに確かめてもらった

**手元の参考図（DXF・DWGとも）には、鏡像で置いた部品が1つも無かった。**
DWGを変換して調べても、マイナスの拡大率も、押し出しZ=-1も、0個だった。

そのため、直したことは**仕様どおりに組み立てた見本でしか確かめられなかった。**

→ **2026-09-08 ユーザーが実機で確認：「確実に治っています」。**

このやり方は使える、と分かった。手元に実物が無くても、

1. **仕様のとおりに、裏返すと必ず形が変わる見本を組み立てる**（47.6）
2. わざと壊して、テストが落ちることを確かめる
3. **「実物では未確認」と、はっきり書いて公開する**
4. ユーザーに実機で見てもらう

の順で進めれば、直せる。**大事なのは3を省かないこと。**
「直りました」とだけ言うと、確かめる必要があることが伝わらない。

### 47.9 覚えておくこと

- **同じことを2通りの持ち方で持たない。** 角度を足し算と行列の両方で持っていたのが原因。
- **裏返すと、回る向きが逆になる。** 円弧・楕円は始まりと終わりを入れ替える。
- **反転は1か所でだけかける。** 2回かかると打ち消し合い、テストも通ってしまう。
- **文字は裏返さない。** 読めないものを出しても、現場では使えない。
- **裏返しても同じに見えるものを、見本にしない。**

---

## 48章 文字の大きさは「大文字の高さ」で決まる（2026-09-08 ユーザーの指摘）

> 「俵型マークの中の文字がセンターにないのと、
> 左下の四角の中の文字の大きさが反映されていない」

**別々に見える2つは、原因が同じ1つだった。**

### 48.1 CADの「高さ」と、画面の「大きさ」は別のもの

| | 何の大きさか |
| :--- | :--- |
| CADの文字の高さ | **大文字（A・8など）そのものの高さ** |
| Canvasの `font` / PDFの `Tf` | **文字枠ぜんたい（em）の大きさ** |

大文字が文字枠に占めるのは**7割ほど**しかない。
そのままCADの高さを渡すと、こうなる。

- 文字が指定より**3割ちかく小さく**出る → 「大きさが反映されていない」
- 書き出しの線（ベースライン）は合っているので**下に寄って**見える → 「センターにない」

囲みの中の文字は、この2つが重なって**小さくて下寄り**になる。
だから「センターにない」と「大きさが違う」が、同時に起きていた。

### 48.2 実物で確かめた

俵型マークを、図形1つずつ座標で測った（文字の書き出しの点を原点として）。

```
下の辺  y = -7.5      上の辺  y = +42.5     ← 囲みの高さ 50
文字の高さ 35、書き出しの線は y = 0
```

CADなら大文字が y=0 から y=+35 まで伸びるので、上下の余りは **7.5ずつ**。
**ぴったり中央**になる。

ところがこちらは em を35にしていたので、大文字は y=+25 までしか伸びず、
上が17.5・下が7.5の**下寄り**になっていた。

> **「ずれている」と言われたら、囲みの座標を測る。**
> 目で見て直そうとすると、勘で動かすことになって決着しない。

### 48.3 直し方：割合を1つだけ決めて、画面も紙も同じものを使う

`drawing.js` に `CAP_HEIGHT_RATIO`（0.72）を置き、
`render.js`（画面と印刷の絵）と `print-pdf.js`（PDF）の**両方がそれを取りに行く**。

渡すときに、この割合で**割る**。

**実測せずに決め打ちにした理由**は、画面と紙で別々に測ると
わずかでも食い違ったときに「確認画面では合っているのに紙だけずれる」からである（36.2）。
画面もPDFも Helvetica系の書体で、大文字の割合はどちらも0.72前後なので、
1つの数を両方で使うほうが安全。

### 48.4 「読める大きさか」の判断は、大文字の高さのまま

小さすぎる文字を描かない目安（5px）は、**大文字の高さ**で測る。
文字枠の大きさで測ると、4px（読めない）が 4÷0.72＝5.6px となり、
**読めない文字まで描いてしまう。**

これは**境目の大きさでしか分からない。**
ふつうの大きさの文字で試すと、どちらで判断しても同じ結果になり、取り違えを見逃す。
わざと壊す確認で、実際に1つすり抜けた。

> **見分けのつく見本を選ぶ。** 47.6と同じ話が、また出た。

### 48.5 ついでに直した：ふつうの文字の位置ぞろえ

46.3で属性の文字（ATTRIB）だけ位置ぞろえ（コード72・74）を読むようにしていたが、
**ふつうの文字（TEXT）は読んでいなかった。**
同じ「中央ぞろえ」でも、図面のどこに書かれたかで結果が変わる状態だった。

同じ関数を使うようにそろえた。

　　なお、**ぞろえの指定が無いのに 11・21 が書いてある図面がある**（実物がそうだった）。
　　そこを置き場所にすると、文字が原点へ飛ぶ。指定があるときだけ使うこと。

### 48.6 日本語の文字は、少し大きくなる

漢字・かなには「大文字の高さ」という考えが無く、文字枠いっぱいに近い。
割り戻すと、Latinの文字より少し大きく出る。

CADも漢字を大文字より少し大きく描くので、**前より近くなっている。**
実物の参考図（北・南・東・西）で、大きくなりすぎていないことを目で確かめた。

### 48.7 覚えておくこと

- **「高さ」が何の高さかを、渡す先ごとに確かめる。**
  CADは大文字の高さ、CanvasとPDFは文字枠の大きさ。同じ言葉で違うものを指している。
- **別々に見える不具合が、1つの原因ということがある。**
  「センターにない」と「大きさが違う」は、同じ1行から出ていた。
- **画面と紙で使う数は、1か所に置いて両方が取りに行く。**（36.2）
- **境目でしか分からないことは、境目の見本で試す。**

---

## 49章 書き足す文字の大きさを、数字で指定する（2026-09-08 ユーザーの指示）

> 「あと文字の大きさを大中小でわなくポイントで変えれればいい」

### 49.1 「ポイント」が何を指すかは、作る前に聞いた

数字で入れられるようにするのはよいが、**その数字が何の大きさかで作りが変わる。**
候補は3つあった。

| 何の大きさか | どうなるか |
| :--- | :--- |
| **図面のミリ** | CADと同じものさし。図面の寸法文字と揃えられる |
| 画面で見えている大きさ | 今までの作りの延長。置いた本人の感覚には合う |
| 紙の上のポイント | **正確には決められない**（下を参照） |

**紙の上のポイントは、置く時点では決められない。**
このアプリの印刷は「囲んだ範囲をA4いっぱいに広げる」ので、
**あとで囲む範囲の広さによって、紙の上の大きさが変わってしまう。**
狭く囲めば大きく、広く囲めば小さく出る。

できないことを「できます」と言わずに、先に伝えて選んでもらった。
ユーザーは**図面のミリ**を選んだ。

> **作り方が分かれる問いは、作る前に聞く**（43章）。
> あわせて、**できないものは「できない理由」ごと出す。**
> 3つ並べて初めて、選びようがある。

### 49.2 何が変わったか

- `notes.js` … 「小・中・大（画面の何ピクセル）」をやめ、**図面のミリ**で持つ
- 窓の3つのボタン → **数字を入れる欄**（`inputmode="decimal"` で数字のキーボードが出る）
- 入れた数字が、そのまま図面の上での文字の高さになる

43.2で「画面で見えている大きさで決める」としていたのを、ユーザーの指示で変えた。
**43.2の考え方が間違っていたわけではない**（置いた本人の感覚には合っていた）。
図面の寸法文字と揃えたい、という現場の要求のほうが強かった。

### 49.3 最初に出す数字は、今の拡大率から決める

図面の縮尺はまちまちである。決め打ちの数を出すと、
**点のように小さい**か**画面を埋め尽くす**かのどちらかになる。

そこで、今見えている拡大率から「画面で20pxに見える大きさ」を計算して入れておく。
2つめからは前に入れた値を引き継ぐので、続けて書くときに入れ直さなくてよい。

　　**上から2けたで丸める。** 「37.42184…」と出されても、直そうという気にならない。

### 49.4 全角の数字も受ける

iPadの日本語キーボードでは、うっかり「３．５」と全角で入ってしまうことがある。
そこで弾くと、**なぜ入らないのか分からない。** 半角に直してから読む。

### 49.5 おかしい数字のまま決定させない

数字でない・0以下・けた違いのときは、**窓を閉じない。**
その場に理由を出して、直してもらう。

　　黙って前の大きさで置くと、**入れたはずの数と違うものが出て、理由が分からない。**
　　「勝手に直す」は親切に見えて、いちばん困る。

判定は**下限と上限の1か所**にまとめた。同じことを2か所で判定すると、
片方を直したときにもう片方が取り残される。
（わざと壊す確認で、重複した判定が「壊しても落ちない」形で残っているのが分かった）

### 49.6 古い記録は、そのまま開ける

前に書いた注記には「小・中・大」の名前が入っているが、
**figureの高さ（ミリ）も一緒に覚えてあった。**
そちらを使うので、古い注記は**同じ大きさのまま開く。**

大きさの名前は、もう覚えない。読むときも見ない。

### 49.7 覚えておくこと

- **できないことは、できない理由ごと先に出す。** そのうえで選んでもらう。
- **決め打ちの初期値を出さない。** 図面の縮尺はまちまち。今の見え方から決める。
- **勝手に直さない。** 入れた数と違うものが出るのが、いちばん分からない。
- **同じ判定を2か所に書かない。** 片方が取り残される。
- **古い記録が開けるか、毎回確かめる**（44.6と同じ）。

---

## 50章 並べ置き（同じ部品を格子状にくり返す）に対応する（2026-09-08 ユーザーの指示）

> 「図面の表示通りに表示したい」

### 50.1 これは「読めない図形」ではなく「読み飛ばし」だった

CADには、同じ部品を格子状にまとめて置く書き方がある（AutoCADでは MINSERT）。

| やり方 | ファイルの中身 |
| :--- | :--- |
| ふつうに6回置く | 「ここに1個」が**6つ**書かれる |
| 並べ置き | 「ここに1個。**横3・縦2でくり返す**」という**1つの指示**だけ |

コード70（横に何個）・71（縦に何個）・44（横の間隔）・45（縦の間隔）である。

これまでは、このくり返しの指示を**読み飛ばして1個だけ描いていた。**

**ここがいちばん悪かった。** 読み飛ばしたことにアプリ自身が気づかないので、
「表示できませんでした」にも出ない。**6個あるのに1個しか出ず、何も知らせない。**

> **「対応していない図形」より、「読み飛ばしている指示」のほうが危ない。**
> 前者は数えて知らせられるが、後者は気づく手がかりが無い。

現場で困るのは、**ボルトやサポートの本数が実際より少なく見える**こと。
数を拾う図面では致命的になりうる。

### 50.2 間隔の決まりは、仕様で確かめてから書いた

**並べる向きは回転（コード50）に従うが、拡大率や鏡像は掛けない。**

拡大率まで掛けると、部品だけでなく**並びの間隔まで伸び縮み**して図面と合わなくなる。
ここは実物が手元に無かったので、仕様の記述を確かめてから実装した。

　　38.3(a)のハッチングでも、46.7の引出線でも「掛けるか掛けないか」で迷った。
　　**数字を掛ける前に、必ず出どころを確かめる。**

### 50.3 上限を置き、打ち切ったぶんは数える

壊れた図面に「横1000個・縦1000個」と書かれていると、そのまま並べると固まる。
1つの並べ置きにつき **1000個まで**（`MAX_ARRAY_COPIES`）。

打ち切ったぶんは**黙って捨てず、1個ずつ数えて画面に出す**（10.5）。
`INSERT（並べ置きが多すぎるため打ち切り）` として出るので、
「本当は何個あるはずだったか」が分かる。

ハッチング（38.3(e)）・自由曲線（45.3(d)）と同じ考えで、**当たらないのが正常。**

### 50.4 くり返しの数が0でも、1個は出す

壊れた図面で `70=0` と書かれていることがある。
そのまま0個にすると、**部品がまるごと消える。** 最低1個は出す。

### 50.5 見本は「取り違えが分かる」ように3通り作った

1つの置き方だけで試すと、間違っていても同じ結果になることがある。

| 見本 | これで何が分かるか |
| :--- | :--- |
| ① 横3・縦2 | 数と位置が合っているか。横と縦の間隔を取り違えていないか |
| ② 90度回して3個 | **並ぶ向きが回転に従うか**（回さないと横のまま出る） |
| ③ 2倍で2個 | **間隔に拡大率を掛けていないか**（掛けると330が360になる） |

わざと11通りに壊して、全部で落ちることを確かめた。

### 50.6 実物では確かめられていない

**手元の2枚（参考図・お客様のDWG）には、並べ置きが1つも無かった。**
この書き方は最近のCADではあまり使われない（「配列複写」しても、
ふつうは1個ずつ別々に置かれる形で保存されるため）。

そのため仕様どおりに組み立てた見本でしか確かめられていない。46.5・47.8と同じ扱い。

### 50.7 覚えておくこと

- **「読み飛ばしている指示」は、数えて知らせることすらできない。**
  対応していない図形より見つけにくい。ときどき棚卸しする。
- **数字を掛ける前に、出どころを確かめる。** 回転は掛ける、拡大率は掛けない。
- **数がいくらでも増えうるところには、必ず上限を置く。** 打ち切ったら数える。
- **0が来たときに、まるごと消えないようにする。**
- **見本は「取り違えが分かる」ように分けて作る**（47.6・48.4と同じ）。
