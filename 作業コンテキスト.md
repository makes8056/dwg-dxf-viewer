# 作業コンテキスト（DWG・DXFビューア）

このファイルは、作業を始めるAIが最初に読むものです。
新しいことが決まったら、必ずここに追記してから先へ進みます。

## いまの状態

- 2026-09-02 フォルダーを作成。
- 2026-09-02 ユーザーと仕様を確認し、下の「決まったこと」を確定。
- 2026-09-02 **第1段階（DXFを開いて見る）が動くようになりました。版番号 v0.1.0。**
- **公開先：https://makes8056.github.io/dwg-dxf-viewer/** （2026-09-03 公開）
- **GitHubリポジトリ：https://github.com/makes8056/dwg-dxf-viewer** （公開・GPL-3.0）
  - 公開先は GitHub Pages。ブランチ main の一番上の階層をそのまま配信している。
  - Cloudflare Pages ではなく GitHub Pages を選んだ理由：
    アカウントの新規登録が要らないこと、設定を司令塔側で完結できること、
    04で起きた「/index.html を / へ転送されてiPadでアプリが開かなくなる事故」の
    原因そのものを避けられること（対策コードは念のため入れる）。

### 第1段階でできたこと（2026-09-02）

- DXFファイルを開いて画面に描く（直線・折れ線・円・円弧・文字）
- ブロックの展開（位置・拡大率・回転・入れ子）
- Shift-JISで書かれた日本語の図面が読める
- レイヤーの色・図形ごとの色・24ビットの色に対応
- 押し出し方向がマイナスの図形の左右反転に対応
- 表示できなかった図形を種類ごとに数えて画面に出す
- スワイプで移動、ピンチ／ホイールで拡大縮小、全体表示

テスト：**59件通過・0件失敗**（`node --test` で実行）。
すべて「わざと壊すと本当に失敗するか」を確かめてあります（開発ルール6.3）。

### 第1段階で実際に見つかった不具合（実物を見て発見。開発ルール7.3）

ワーカーが作ったものをブラウザで動かし、座標を測って2件見つけて直しました。

1. **隠したはずの案内が画面に出たままだった。**
   `hidden`（表示しない印）より、CSSの `display: flex` のほうが強いため。
   `ui.css` に `[hidden] { display: none !important; }` を追加して解決。
2. **案内の帯がボタンに13ピクセル重なっていた**（04と同じ事故）。
   案内の位置がボタンの高さを「84px」と決め打ちしていたのが原因。
   `app.js` で本当の高さを測り、CSSの `--toolbar-height` に渡す形に変更。
   横向き（1024×768）でも縦向き（768×1024）でも、すき間12ピクセルで重ならないことを実測で確認。

**どちらもテストは全部通っていました。** テストだけでは見つかりません。

### 実際の図面で見つかった不具合（v0.1.1／2026-09-03）

ユーザーが `参考図.dxf`（5MB・63万行・AutoCAD 2018形式）を置いてくださいました。
**手で書いた見本では見つからない不具合が、すぐに2件出ました。**

1. **日本語が全部化けていた。**
   この図面のヘッダーには「文字コードはShift-JIS（ANSI_932）」と書いてあるのに、
   中身は実際にはUTF-8でした。新しいAutoCAD（AC1021＝2007以降）はこう保存します。
   申告を信じたため、レイヤー名「図面枠」が「蝗ｳ髱｢譫」のようになっていました。
   → 申告を鵜呑みにせず、版と中身から判断する形に変更。
2. **「45%%D」がそのまま画面に出ていた。**
   CADで「45度」を表す書き方です。→ `°` `φ` `±` `%` に置き換えるようにした。

再発防止のテストを追加（`tests/fixtures/utf8-mislabeled.dxf`）。テストは62件になりました。

### 参考図.dxf を読ませた結果（2026-09-03）

- 読み込み時間：**139ミリ秒**（5MBでこの速さなら、iPadでも問題なし）
- 図形：430個（直線331・円56・文字25・円弧17・折れ線1）
- レイヤー：51個（既設PIPE／既設SUPPORT／埋設／撤去 など、日本語が正しく読める）
- **表示できなかった図形：40個**
  - `DIMENSION`（寸法）32個 ← **次に対応したい。管工事では寸法が読めないと困る**
  - `VIEWPORT` 4個 ／ `ELLIPSE` 2個 ／ `HATCH` 1個 ／ `POINT` 1個
- 寸法は、図面の中に「寸法用の部品（*D…）」として50個入っていることを確認済み。
  この部品を展開すれば、寸法線・矢印・寸法値がそのまま描けます。

### 寸法の表示と「真っ白事故」の修正（v0.1.2／2026-09-03）

**1. 寸法（DIMENSION）を表示できるようにしました。**

CADは寸法を「線・矢印・数字」の部品として図面の中に持っていて、
DIMENSIONという図形は、その部品の名前（例 `*D88`）を指しているだけでした。
実物で調べたところ、**部品の中身はすでに図面と同じ座標で書かれていた**ので、
位置をずらさずそのまま展開すれば正しい場所に出ます。

結果：図形 430個 → **696個**、文字 25個 → **57個**（増えた32個が寸法値そのもの）。
表示できなかった図形：40個 → **8個**。

※寸法の部品の中にある `POINT` は、CADでも印刷されない目印なので数えていません。
　数えると実物では97個になり、**本当に足りない図形が埋もれてしまう**ためです。

**2. 【重大】実物の図面が真っ白にしか見えない不具合を直しました。**

参考図.dxf には、図面本体から**とんでもなく離れた場所に図形が3個**ありました。

| | 範囲 |
| :--- | :--- |
| はぐれ図形を含む全体 | 262726 × 133487 |
| 図面本体だけ | **10639 × 5542** |

全体を画面に収めようとすると、はぐれ図形に引っぱられて
**図面本体が5ピクセルほどの点になり、画面が真っ白にしか見えませんでした。**

対策：`drawing.js` に `computeContentBounds()` を追加し、
「多くの図形が集まっている場所」を図面本体とみなして全体表示に使います。
判定はかなり控えめで、**98%の図形が収まる距離の10倍より遠いものだけ**を外します
（図面枠のように四隅にしかない図形を、うっかり切り落とさないため）。

はぐれ図形があることは**画面に出して伝えます**（黙って隠すと「図面の一部が無い」と
誤解されるため）。

テストは72件になりました。「甘すぎる判定」「厳しすぎる判定」の両方で
本当に失敗することを確認済みです。

### 【iPadで判明】図面を開くでDXFが選べない不具合（v0.1.3／2026-09-03）

ユーザーが実機のiPadで試したところ、**「図面を開く」を押しても
DXFファイルが選べませんでした**（灰色になって押せない）。

**原因：`file-open.js` の `input.accept = '.dxf,.dwg'`**

iPadのファイルアプリは、`accept` に書いた拡張子を
「iPadが知っているファイルの種類」に読み替えてから絞り込みます。
ところが `.dxf` と `.dwg` は iPad が知らない種類なので読み替えに失敗し、
**結果としてすべてのファイルが選べなくなります。**

対策（`src/ui/file-open.js`）：

1. **`accept` を付けない。** 種類を絞らず、どのファイルでも選べる状態にする
   → **絶対に付け直さないこと。** 現場で図面が開けなくなります
2. 選ばれたあとに**中身の先頭を見て判断する**ようにした
   - DWGは先頭が `AC10xx`（拡張子が違っても分かる）
   - DXFは先頭のほうに `SECTION` が出てくる
   - 拡張子が違っていても中身がDXFなら開ける。写真などは日本語で案内する
3. 隠し方も変更。`left:-9999px` で画面外へ飛ばすのをやめ、
   透明にして1ピクセルだけ置く形にした
   （iPadでは完全に消した入力欄を押しても、選択画面が開かないことがあるため）

**この不具合は、パソコンのブラウザでは絶対に再現しません。** 実機でしか分かりません。

### 【iPadで判明】3件まとめて修正（v0.1.4／2026-09-03）

ユーザーが実機のiPadで使って報告してくれた3点。**どれもパソコンでは再現しませんでした。**

**1. 図面が画面の左上4分の1にしか出ない（いちばん重大）**

画面の拡大の倍率（iPadは2倍）を、`app.js` と `render.js` の**両方が別々に設定**していて、
あとから動く `render.js` が**1倍で上書き**していました。
2倍の広さのキャンバスに1倍で描くので、左上4分の1に縮こまります。

パソコンの画面は1倍なので、**私のテストでは絶対に出ませんでした。**

対策：倍率を決める場所を `render.js` の1か所にまとめ、
呼び出す側が渡し忘れても**キャンバスの大きさから自分で計算する**ようにした。
`tests/render.test.js` を新設し、Canvasのふりをする入れ物で2倍・3倍を検査する。

**2. 寸法の数字だけ、いつも水平に出ていた**

ふつうの文字（TEXT）は「何度傾ける」で向きが決まりますが、
**MTEXTは向きを矢印（コード11・21）で書くことが多い**のを見ていませんでした。
あわせて「書き出す点のどこに文字を置くか」（コード71）も読むようにし、
寸法の数字が線の中央にそろうようにしました。

結果：実物の図面で**25個の文字が正しい向き**になった（300→90度、100→30度、424→315度 など）。

**3. 動的ブロックの切り替えが効かず、全部の形が重なって出ていた**

AutoCADの「動的ブロック」は、ひとつの部品の中にありうる形を**全部まとめて持たせておき**、
「今回はこの形」という指定で切り替えます。使わない形には「見えない」印（コード60が1）が付きます。
この印を読まずに全部描いていました。

参考図.dxf には**「見えない」指定の図形が805個**ありました。
あわせて、非表示（OFF）・凍結（FROZEN）のレイヤーも出さないようにしました。

結果：図形 696個 → **605個**（91個を除外）。レイヤー「関節位置」も除外。

テストは93件になりました。4通りのわざと壊す確認を行い、すべて狙いどおり失敗しました。
再発防止のため `開発ルール.md` に14章・15章・16章を追加。

### 【iPadで判明】エルボだけ黒く出ていた（v0.1.5／2026-09-03）

ユーザーから「アイソメ図のエルボの色が違う。配管は赤なのにブロック部分だけ黒」と報告。

**原因：AutoCADの「ブロックの中身がレイヤー0なら、置いた側のレイヤーに従う」決まりを
実装していなかった。**

実物の図面を調べたところ、こうなっていました。

| | レイヤー |
| :--- | :--- |
| エルボの部品を**置いた場所**（INSERT） | **PIPE（赤）** |
| 部品の**中身**が描かれている場所 | **0（黒）** |

AutoCADには「ブロックの中身がレイヤー0に描かれている場合、その中身は
**その部品を置いた側のレイヤーに従う**」という決まりがあります
（部品を色違いで使い回すための仕組み）。
これを知らずにレイヤー0（黒）で描いていたため、エルボだけ黒くなっていました。

対策：`effectiveLayer()` を追加し、レイヤー0の中身は置いた側のレイヤーで扱う。
表示・非表示の判定（15章）にも同じ実効レイヤーを使う。

**やりすぎ注意：** レイヤー0**以外**に描かれた中身は、そのレイヤーのままにすること。
ここまで置いた側に従わせるとレイヤー分けが壊れます。両方向のテストを入れました。

結果：PIPEレイヤーの図形136個が赤くなりました。テストは96件。

### 楕円に対応・案内の数え方を見直し（v0.1.6／2026-09-03）

ユーザーから「表示できませんでした8個」の説明を求められ、中身を調べたうえで対応しました。

**内訳と判断**

| 種類 | 数 | 判断 |
| :--- | ---: | :--- |
| `VIEWPORT` | 4 | **図面の線ではない**（印刷レイアウトののぞき窓）→ **数えるのをやめた** |
| `ELLIPSE` | 2 | **本物の図形。図面が欠けていた** → **対応した** |
| `HATCH` | 1 | 斜線の網掛け。実害が小さいので後回し |
| `POINT` | 1 | 点1つ。ほぼ影響なし |

**結果：「表示できませんでした」が 8個 → 2個（HATCH 1・POINT 1）になりました。**

**楕円の実装で気をつけたこと**

- `11,21` は長いほうの軸の端だが、**中心からの相対位置**（絶対位置ではない）
- `41,42` は**ラジアン**。度に直す
- **始まりと終わりが同じ角度なら「ぐるっと一周」の意味。**
  角度を0〜360度に直すと一周の楕円は「0度→0度」になり、
  そのまま描くと**何も描かれない。** 実物には一周の楕円が115個あるので必須。
  実装中に自分で気づいて、テストを足して直しました。

実物の図面で確認：赤いC字の楕円2個（305度→235度、290度ぶん）が正しく描かれました。

テストは104件。5通りのわざと壊す確認をすべて実施。
再発防止のため `開発ルール.md` に18章（案内に数えてよいもの・いけないもの）と
19章（楕円の扱い）を追加。

### 図面を覚えておく・版番号を右下へ・オフライン対応（v0.1.7／2026-09-03）

ユーザーから実機で3点の要望。**この回からワーカー（ソネット）を使い始めた。**
ユーザーの指示：「ワーカーが使えるところはワーカーで。オーパスより使用料が節約できる」。
担当ファイルが重ならないよう分け、**つなぎ込みは司令塔がやる**
（04の「作ったのに呼ばれていない」事故を避けるため）。

**1. 開いた図面を覚えておく（`src/storage.js`。ワーカー1が作成）**

更新のたびにファイルアプリから開き直すのは現場で使えない、という報告。

- 覚えるのは**元のファイルのバイト列**（解析済みではない。開発ルール20.2）
  → 読み取りの作りを直したとき、覚えている図面にも新しい読み方が自動で効く
- IndexedDBに最大5件。起動時にいちばん最近の図面を自動で開き直す
- **覚えられなくてもアプリは普通に動く**（プライベートブラウズ・容量オーバー）

ワーカーが調べて分かったこと：**Safariの「7日間使わないとデータを消す」決まりは、
ホーム画面に追加したアプリには適用されない。** この使い方なら図面は残りやすい。
ただし空き容量が少ないときの強制削除は別枠で起こりうるので、消えても平気な作りにしてある。

**2. 版番号を右上→右下へ（司令塔が実施）**

iPadの右上は電池残量や電波状況の表示と重なるため。
ボタンの列と重ならないことを実測で確認。狭い画面ではボタンが右まで伸びて重なるので、
**実測して自動で上に逃がす**ようにした（数字を決め打ちしない。開発ルール7.4）。

| 画面 | 結果 |
| :--- | :--- |
| iPad横 1024×768 | 右から10px・下から10px、重なりなし |
| 狭い画面 390×700 | 自動で87px持ち上げ、すき間9px |

**3. パソコンのChromeが更新されない（→ PWAで根本対応。開発ルール21章）**

調べたところ **GitHub Pages は `Cache-Control: max-age=600`** を返していた。
ブラウザが10分間、古いファイルを使い続ける。
さらに**ファイルごとに別々にキャッシュされる**ので、
「app.js は新しいのに render.js は古い」という**混ざった状態**が起こりうる。
版番号を見ても気づけないので危険。

→ Service Worker で「アプリ本体をまとめて1組」として扱う（ワーカー2が作成）。
　ついでにオフライン対応も入った。

**PWA・オフライン対応（`service-worker.js` ほか。ワーカー2が作成）**

- 04寸法取りアプリの `service-worker.js` を土台にした（本番事故の対策ごと引き継ぎ）
- アプリ本体17ファイルを**まとめて1組**として事前に読み込む。
  1つでも失敗したら、その更新まるごとを取り消す（混ざった状態を作らない）
- 新しい版が来たら画面に「新しい版があります」と出し、**押されたときだけ**切り替える
- アイコンは `tools/make-icons.mjs` が node標準機能だけでPNGを組み立てる（npm不使用）。
  2回動かして同じものができることを確認済み（開発ルール6.4）

**実物での確認（本物のChromeで実施）**

このセッションのブラウザ道具はService Workerを登録できない環境だった
（1行だけのSWでも登録できないことを司令塔が自分で確認）。そこで本物のChromeで確認した。

1. Service Workerが有効になり、**17ファイルがキャッシュされた**（一覧と完全一致）
2. **サーバーを完全に停止**（接続できないことをcurlで確認）
3. その状態でページを開き直す → **アプリが完全に表示された**
4. さらにオフラインのまま図面を開き、もう一度開き直す →
   **覚えていた図面が復元されて描画された**（`dxf-parse.js` もキャッシュから読まれた）

**自動テストで見張るようにしたこと（`tests/offline.test.js`）**

手で合わせる決まりは、いつか必ず合わせ忘れます。だから機械に見張らせます。

| 見張る内容 | 外すと何が起きるか |
| :--- | :--- |
| `CACHE_VERSION` と `APP_VERSION` の一致 | 直しても古い画面が出続ける（04の事故） |
| 事前読み込み一覧の数え漏れ | そのファイルだけオフラインで読めない |
| 一覧に実在しないファイル | **新しい版が永遠に届かなくなる** |
| リダイレクト対策が2か所にある | iPadのホーム画面から**アプリが開かなくなる**（04の事故） |
| `start_url` が `"./"` | 同上（04の事故） |
| install で `skipWaiting` を呼ばない | 図面を見ている最中に画面が勝手に変わる |

5通りのわざと壊す確認を実施し、すべて狙いどおり失敗した。テストは129件。

**注意：v0.1.7への更新は、まだ古いやり方で届く。**
v0.1.6にはService Workerが入っていないため。
v0.1.8以降から「更新する」ボタンが出るようになる。

### 裏に回っている間の更新／見えない文字（v0.1.8／2026-09-03）

ユーザーがiPadで確認。**v0.1.7の3点はすべて正常動作**との報告。あわせて2点の指摘。

**1. アプリを裏に回している間に更新が来ても気づかない**

原因：**アプリが裏に回ると、iPadが時計を止める。**
そのため「30分ごとに見に行く」が動かない。
アプリを完全に終了して立ち上げ直せば更新されるが、裏から戻っただけでは気づかない。

対策（`src/update-check.js`）：**表に戻ってきた瞬間に見に行く。**
`visibilitychange`（他のアプリから戻った）・`focus`（別ウィンドウから戻った）・
`pageshow` の `persisted`（冷凍保存されたページの復活）の3つを見張る。
短い間に何度も見に行かないよう、最短30秒の間隔を設けた。

**2. 「図面から遠く離れた場所に3個の図形があります」がCADで見つからない**

調べたところ、**3個のうち1個は「中身が空っぽの文字」**だった。
書式の指定（`\A1;`）だけが入っていて、肝心の文字が無いMTEXT。
**CADの画面には何も出ない。** 探しても見つからなくて当然だった。

対策：**中身が空の文字は、図形として作らない**（`emitText`）。

これには**思わぬ副次効果**があった。見えない文字が図面の範囲を広げていたため、
図面本体の範囲が **10639×5542 → 5441×4161** に縮んだ。
つまり**図面が2倍近く大きく表示される**ようになった。

残り2個は本物の文字だった（どちらも打ち間違いと思われる）：

| 中身 | レイヤー | 座標 |
| :--- | :--- | :--- |
| `outlook` | 0 | -141460, 86849 |
| `jjggggj` | 0 | -11548, 103530 |

あわせて、案内の文言を**「何が・どのレイヤーの・どこにあるか」まで書く**ように変更。
「3個あります」だけでは、CADで探しても見つけられない（実際に見つけられなかった）。

テストは132件。開発ルールに22章（更新にいつ気づくか）・23章（見えないものを
「ある」と報告しない）を追加。

### 図面の一覧・10件・混乱する案内をやめた（v0.1.9／2026-09-03）

ユーザーがiPadで確認。**v0.1.8の3点はすべて正常動作**との報告。あわせて3点の要望。

**1. 「図面から遠く離れた場所に◯個の図形があります」の案内をやめた**

ユーザーの言葉：「**CADでは描かれていないなら表示しなくていい。混乱します**」。

中身は正しかったが、**読んだ職人さんにできることが何も無い案内だった。**
図面本体に合わせて表示する**動作はそのまま**。案内だけをやめた。
はぐれ図形の中身は `drawing.outlierList` に残してあるので、不具合調べには使える。

同じ判断ミスを繰り返さないよう、開発ルール25章に基準を明文化。
**「これを読んだ職人さんは、何をすればよいか分かるか」で判断する。**

**2. 覚えておく図面を5件→10件に**

いっぱいになったら古いものから消える（これは元からの動作）。

**3. 覚えている図面から選ぶ一覧を追加（`src/ui/drawing-list.js`。ワーカーが作成）**

ツールバーに「図面を選ぶ」を追加（ボタンは5つになった）。
ファイルアプリを経由せず、アプリの中で図面を切り替えられる。
1件ずつ消せる。消すときは必ず確認を出す（誤って消すと電波が無いと取り戻せないため）。

**つなぎ込み中に見つけた不具合（司令塔が発見・修正）**

文字の縦位置に **`baseline` というCanvasに存在しない名前**を渡していた。
存在しない名前は**ブラウザに黙って無視される**ため、直前に描いた文字の縦位置が
そのまま残り、文字がずれて出る。ブラウザの記録には警告が出ていたが、
画面には出ないので気づきにくかった。正しい名前 `alphabetic` に修正。

**実物での確認**

- 図面を3枚覚えさせ、一覧から切り替え → **色が変わることで切り替わりを確認**
- 削除 → 確認ダイアログ「『北棟配管図.dxf』を忘れますか？」が出て、
  一覧からも記憶からも消えることを確認
- ボタン5つでも画面に収まる（狭い画面でははみ出す分を横スクロールで押せる）

テストは137件。5通りのわざと壊す確認を実施。

### 範囲指定プリント（v0.2.0／2026-09-03）— **このアプリの一番の目的**

ユーザーに確認：**印刷した紙に定規を当てて測ることは無い**。
そのため縮尺指定（1/50など）は作らず、**用紙に合わせて自動で拡大縮小するだけ**にした。
決めごとは開発ルール26章に書き切ってから着手した。

**できたこと**

- ツールバーに「印刷する範囲」を追加（ボタンは6つ。iPad縦でも収まることを実測）
- 押すと、キャンバスの上に**透明な板**をかぶせて範囲を囲むモードに入る
  （図面を動かす操作とぶつからないようにするため。26.4）
- 指でなぞると半透明の四角が出て、離すと
  「この範囲を印刷」「やり直す」「やめる」の3つが出る
- 押すと**図面データから描き直して**印刷用の絵を作り、iPadの標準の印刷画面を開く
- 紙には**図面だけ**。ボタン・案内・版番号は出ない

**司令塔が見つけて直した設計の誤り（重要）**

ワーカーは絵の大きさを「**囲んだ範囲の実寸 × 200dpi**」で決めていた。
これは「用紙に合わせて拡大縮小」する方式では**誤り**。実測でこうなった：

| 囲んだ範囲 | 絵の大きさ | 紙の上での細かさ |
| :--- | ---: | :--- |
| 100mm×60mm（**小さい部分を拡大。いちばんよくある使い方**） | 787×472 | **71dpi ぼやける** |
| 800mm×500mm | 4000×2500 | 362dpi |

用紙に合わせる方式では、最後に紙いっぱいへ引き伸ばすので、
絵の大きさは**実寸ではなく紙の大きさ**から決めなければいけない。
直した結果、**どんな範囲でも紙の上でちょうど200dpi**になった（上限にも当たらない）。

ワーカーは「紙の大きさから決めると上限（長辺4000点・全体1200万点）が
絶対に発動しなくなる」ことを理由に実寸方式を選んでいたが、
**上限はiPadで落ちないための安全装置であって、当たらないのが正常。** 目的を取り違えていた。

**実物での確認**

- 参考図.dxf を読み込み、範囲を囲んで「この範囲を印刷」まで通した
- できた絵（2212×1382）を画面に出して目視 →
  配管（赤）・溶接記号・寸法「250」まではっきり描かれていることを確認
- 印刷後に、囲む画面と印刷用の入れ物が片付くことを確認

**自動テスト（`tests/print.test.js` を新設）**

印刷の設定が壊れても**画面を見ているかぎり誰も気づかない**（紙に出して初めて分かる）ので、
機械に見張らせた。

| 見張る内容 | 外すと何が起きるか |
| :--- | :--- |
| 印刷時に画面のものを全部消す | **ボタンや案内が紙に印刷される** |
| 印刷用の入れ物だけを出す | 紙が真っ白になる |
| 図面が歪まない・はみ出さない指定 | 図面が歪む／端が切れる |
| どんな範囲でも紙の上で200dpi | 小さい範囲を印刷するとぼやける |
| 線の太さが印刷用の値 | 線が細すぎて紙で見えない |

テストは166件。5通りのわざと壊す確認を実施。

### 実物の紙で判明した不具合（v0.2.1／2026-09-04）

ユーザーが**実際に印刷した紙をスキャンして見せてくれた。**
その1枚で、画面では絶対に気づけない不具合が3つ見つかった。

**1. 図面が1本も印刷されていなかった（いちばん重大）**

紙の真ん中に「印刷する図面」という枠だけが出ていた。
これは**絵が入らなかったときに出る代わりの文字**。つまり印刷は失敗していた。

原因：iPadのSafariでは `window.print()` が**すぐに戻ってくる**（印刷画面は後から開く）。
その直後に絵を片付けていたため、**印刷される前に絵が消えていた。**
パソコンでは印刷画面が閉じるまで待つので、**この不具合はパソコンでは再現しない。**

対策：片付けを `afterprint`（印刷が終わった合図）で行うようにした。

**2. 紙の余白にURL・日付・ページ番号が入っていた**（ユーザーからの指摘）

ブラウザが余白に勝手に入れるもの。`@page` の余白を0にして出ないようにし、
図面が紙の端にくっつかないよう**余白は入れ物の内側（padding）で取る**ようにした。

**3. 私（司令塔）が直す途中で作り込んだ不具合**

1を直すとき「もっと確実に」と `decode()` で待つようにしたが、
**画面に出していない絵では `decode()` が終わらない**（3秒待っても終わらないことを実測）。
その結果、**印刷ボタンを押しても何も起きなくなった。**
時間切れを付けて直した。読み込みの確認は別に行っているので、印刷は止まらない。

**確認**：実物で印刷の流れを最後まで通し、
**印刷の瞬間に絵がちゃんと入っている（2212×1382）**ことを確認した。

テストは171件。4通りのわざと壊す確認を実施。
再発防止のため `開発ルール.md` に27章（印刷の落とし穴）を追加。

**この3つは、どれも画面では正常に見えていた。**
ユーザーが紙を見せてくれなければ、ずっと気づけなかった。

### 印刷のやり方を根本から変えた（v0.2.2／2026-09-04）

v0.2.1でもまだ紙が正しく出なかった。ユーザーからの決定的な報告：

> 印刷画面のプレビューには図面が出ていた。**紙の向きを横に変えたら同じ（白紙）になった。**
> その後キャンセルして同じ作業をしても、**一切図面は現れなかった。**

**原因：印刷画面で設定を変えると、ページが作り直される。**
そのとき「印刷が終わった」合図が飛び、v0.2.1で入れた後片付けが走って絵を消していた。
一度消えると、次からも出ない。**印刷中に後片付けをする設計そのものが間違いだった。**

さらに調べたところ、**iPadのSafariは紙の余白のURL・日付をCSSでは止められない。**

**新しいやり方（開発ルール28章。ユーザー了承済み）**

**ページを印刷するのをやめ、「図面の絵そのもの」をiPadに渡す。**

1. 範囲を囲む（ここまでは同じ）
2. 「この範囲を印刷」を押す
3. **アプリの中に、印刷される絵をそのまま大きく出す**（押す前に確認できる）
4. 「プリント」を押すと、iPadの共有メニューが開く
5. そこから「プリント」を選ぶ

これで：

- **URL・日付・ページ番号は一切出ない**（ページを印刷しないため）
- **白紙になる危険が無い**（絵を先に作り、目で見てから渡す）
- **印刷される絵を押す前に確認できる**（現場で紙を無駄にしない）

**気をつけた点**：共有メニューを開く処理は、**指で押した流れの中で待たずに呼ぶ**。
iPadは間に待ち時間が入ると共有メニューを開かせないことがある。
そのため絵は先に作っておき、押した瞬間は渡すだけにした。

**あわせて直した弱点**

画面の大きさが決まる前に全体表示を計算すると、**図面が点のように小さくなり、
そのまま直らない**状態になることを確認した。
まともな大きさになった最初の1回だけ計算し直すようにした
（ユーザーが動かした位置を勝手に戻さないよう、1回だけ）。

**確認したこと**

- 確認画面が出て、絵（2212×1369）と「やめる」「プリント」が出ることを確認
- 画面座標→図面座標の変換が**往復で完全一致**することを確認
- その範囲で作った絵に**線が39249点**入っていることを確認

テストは175件。3通りのわざと壊す確認を実施。

**まだ確かめられていないこと**：**実際に紙に出した結果は司令塔には確認できない。**
**自動テストが早速仕事をした**：ワーカーの新しい2ファイルがオフライン用の一覧に
入っていないことを検出。入れ忘れたまま公開すると、電波の無い現場で一覧が開けなくなっていた。

### 横が切れていた（v0.2.3／2026-09-04）

v0.2.2でURLと日付は消えたが、実物の紙を見ると**図面の左右が切れていた。**

> ユーザー報告：「横が切れている」
> 「印刷設定を標準にして、それからプリントボタンを押したら印刷画面を出して」

**原因（開発ルール29章）**

v0.2.2までは**囲んだ範囲の形をした絵**を渡していた。A4用紙とは形が違う。
iPadのプリント画面は絵を紙いっぱいに引き伸ばすので、絵が紙からはみ出す。
**プリンターは紙の縁3〜5mmに物理的に印刷できない**ため、そこが切れた。
確認画面ではまったく分からない。紙に出して初めて分かる不具合だった。

**直したこと**

1. **絵そのものをA4用紙1枚の形にした**（200dpiで2338×1653点）。
   外周8mmは白い余白。図面はその内側にだけ描く。
   絵と紙の形が同じなので引き伸ばされても位置がずれず、
   縁で失われるのは白い余白だけになる。
2. **囲んだ範囲の外は描かないようにした**（`ctx.clip()`）。
   紙1枚ぶんに広がったぶん、何もしないと範囲外の図形が余白に出てしまい、
   また縁で切れる。
3. **余白の二重取りをやめた**（CSS側の `padding: 8mm` → `0`）。
4. **画面用の「まわり5%のすき間」を打ち消した。**
   そのままだとA4横で左右が8mmではなく20.8mmになり、図面が小さく印刷されていた。
5. **確認画面で紙ぜんたいが見えるようにした。**
   箱の高さが「中身しだい」で、紙の下のほうが見切れていた。

**「印刷設定を標準にして」について**

絵がA4用紙1枚の形になったので、**プリント画面の設定は触らなくてよくなった。**
紙の向きも拡大率もそのままで正しく出る。
確認画面の案内も「A4横向きいっぱいに印刷します。プリント画面の設定は、
そのままでかまいません。」に変えた。
（28.1の白紙不具合も、紙の向きを変えたことが引き金だった。触らせないほうが安全。）

**「プリントボタンを押したら印刷画面を出して」について**

**これはiPadのブラウザからは実現できない。** Webページから
AirPrintの画面を直接開く方法が存在しないため、共有メニューを一度経由するしかない。
唯一の代わりは `window.print()`（ページを印刷する方式）だが、
それはv0.2.1・v0.2.2で失敗した道で、**紙にURLと日付が必ず入る**（28.1）。
そのため共有メニュー経由のままにしてある。ユーザーへ説明済み。

**確認したこと（司令塔が実機ブラウザで実測）**

- 絵の縦横比 1.4144（A4は1.4143）— 紙とぴったり同じ形
- 実際に描かれた線の位置を1点ずつ数えて、余白が**全方向7.7mm以上**あることを確認
  （横長・縦長・正方形・極端に横長の4通り）
- 透明な点は0。紙ぜんたいが白で塗られている
- 確認画面で紙ぜんたいが見切れずに出ることを、パソコン画面とiPad相当の画面で確認

テストは179件（新しく4件追加）。5通りのわざと壊す確認を実施。

**まだ確かめられていないこと**：**実際に紙に出した結果は司令塔には確認できない。**

### 囲んだ範囲を調整できるようにした／ボタンを4つに減らした（v0.2.4／2026-09-04）

> ユーザー要望：「印刷範囲を指定したときに、その指定を調整できるような機能をつけて欲しい」
> ユーザー判断：「拡大縮小のボタン、これは必要ないと思うので削除しておいてください」

**(1) 囲んだ範囲を、あとから調整できるようにした（開発ルール30章）**

一度で思いどおりに囲めることは、まずない。とくに手袋をした指では難しい。
それまでは「囲み直す」しかなく、少しずれただけでも最初からやり直しだった。

- 四角のまわりの丸（つまみ）をつまむ → 大きさを直せる（角4つ・辺の真ん中4つ）
- 四角の中を押したまま動かす → 範囲ごと動かせる
- 「やり直す」は「囲み直す」に名前を変えた（調整と区別が付くように）

気をつけた点：

- つまみの当たり判定は44px以上。見た目の丸（26px）より広くしてある（開発ルール11章）
- 四角が小さいときは、辺の真ん中のつまみを出さない（丸どうしが重なって押せなくなる）
- ボタンの箱が下のつまみを隠すときは、箱を上へ移す
- つまんだまま反対側へ行っても壊れない（左右・上下が入れ替わるだけ）
- 印刷できない大きさにはつぶせない／画面の外へは出ない
- 調整の途中で指が外れても、それまでの範囲は消さない

**司令塔が実機ブラウザで見つけて直した不具合**：
箱の高さを「190pxくらい」と見積もって判定していたら、実測187pxで数ピクセル足りず、
**判定を外して実際につまみが隠れた。** 見積もりをやめ、
実際に置いてみて本当に重なるかを測る作りに直した（30.3(c)）。

**(2) 「拡大」「縮小」ボタンを外した（開発ルール31章）**

iPadでは2本指のつまむ操作で拡大縮小できるので、同じことをするボタンは要らない。
ボタンは4つになった：図面を開く／図面を選ぶ／全体表示／印刷する範囲。
拡大縮小そのものは無くなっていない（src/ui/gestures.js が受け持つ）。
押しても何も起きないボタンや呼ばれない処理が残らないよう、残りかすも消した。

**確認したこと（司令塔が実機ブラウザで実測）**

- ボタンが4つだけになっていること
- つまみ8つすべてが、指を受け取れること（`elementFromPoint` で1つずつ確認）
- 右下のつまみを引いて 352×208 → 560×323 に広がり、左上の角は動かないこと
- 四角の中を動かして、大きさが変わらずに位置だけ動くこと
- 調整後の範囲が、そのまま印刷の確認画面に出ること
- パソコン画面（1280×720）とiPad相当（768×1024）の両方で確認

テストは202件（新しく23件追加）。6通りのわざと壊す確認を実施。

**まだ確かめられていないこと**：**実機（iPad）の指での操作感は司令塔には確認できない。**

**返事待ちのこと**：印刷が遅い件。原因は「紙1枚ぶんの絵（写真）」を渡しているためで、
アプリ側は0.06秒・186KBしか使っていない（実測）。
細かさを200dpiから下げれば軽くなるが、印刷の見た目に関わるので、
ユーザーの判断を待っている。

### 範囲を囲んでいる間も拡大縮小できるようにした（v0.2.5／2026-09-04）

> ユーザー確認：「すべてうまく動作しています」（v0.2.4）
> ユーザー要望：「この範囲指定をしているときに拡大縮小ができない。
> ピンチアウト・ピンチができないので、これを行えるようにしてください」

**原因**：「印刷する範囲」に入ると、キャンバスの上に透明な板をかぶせて指を受ける（26.4）。
この板が指をぜんぶ受け取るので、図面を動かす係（gestures.js）に指が届いていなかった。

**直したこと（開発ルール32章）**

板の上の指を、本数で分けた。

- 指1本 … 範囲を囲む・つまみで直す・範囲ごと動かす（今までどおり）
- 指2本 … 図面そのものを拡大縮小・移動する（ピンチ）
- パソコンでは、マウスのホイールでも拡大縮小できる

**いちばん大事にした点**：拡大縮小しても、**四角は図面の同じ場所に貼り付いたまま**にした。
何もしないと図面だけが動いて四角は画面に取り残され、
「囲んだつもりの場所」と「実際に印刷される場所」がずれる。
紙に出すまで気づけない、いちばんたちの悪いずれ方をする。
図面の座標との行き来は viewport.js だけの仕事なので（10.6）、
app.js から toDrawing / toScreen を渡し、print-ui.js はそれを呼ぶだけにした。

**あわせて直した弱点**：「範囲が小さすぎます」の測り方。
図面ぜんたいを見ようと縮小してから印刷すると、四角は画面上では小さく見える。
そのままだと、範囲は何も変わっていないのに断ってしまう。
「囲んだときの大きさ」で測るように直した（32.3）。

**気をつけた点**

- 2本目の指が触れたら、1本指でやりかけていたことは、なかったことにする
  （つまみを持ったまま2本目を置いても、つまみが動いたままにならない）
- 囲みかけの四角も、2本目の指で捨てる（拡大したいだけなのに細い四角ができないように）
- 2本→1本に減っても、残った指で囲み始めない
- モードから抜けるとき、おさえている指の記録も空に戻す（gestures.js と同じ事故対策）
- 2本指で拡大縮小できることを、画面に文字で出した

**確認したこと（司令塔が実機ブラウザで実測）**

本物の viewport.js と本物の print-ui.js をつないで、指2本の操作を流し込んで測った。

- 2本指を広げると、図面の倍率がちょうど2倍になること
- そのとき四角も2倍になり、**図面上の位置のずれが 0 であること**（＝図面に貼り付いている）
- 印刷に渡る「囲んだときの大きさ」が、拡大前の 300×200 のままであること
- 何も囲んでいない状態でいきなり2本指 → 変な四角ができないこと
- そのあと指1本で、ちゃんと囲めること
- つまみを持った直後に2本目の指 → つまむ前の形（200×150）に戻ること
- 拡大したあとも、指1本で範囲ごと動かせること
- 実際の画面でも、ホイールで3倍に拡大して四角が図面についてくることを目で確認

テストは213件（新しく11件追加）。4通りのわざと壊す確認を実施。

**まだ確かめられていないこと**：**実機（iPad）の指2本の操作感は司令塔には確認できない。**

**返事待ちのこと**：印刷が遅い件（v0.2.3から持ち越し）。
原因は「紙1枚ぶんの絵（写真）」を渡しているためで、アプリ側は0.06秒・186KBしか使っていない。
細かさを200dpiから下げれば軽くなるが、印刷の見た目に関わるのでユーザーの判断を待っている。

### パソコンでも図面を動かせるようにした（v0.2.6／2026-09-04）

> ユーザー確認：「iPadではすべてOK」（v0.2.5）
> ユーザー報告：「パソコンChromeでは拡大縮小はいいが移動できない。iPadでは指2本で動かせる」

**原因（開発ルール33章）**

v0.2.5で「指2本＝図面を動かす」にしたが、**パソコンには指2本が無い。**
拡大縮小はホイールでできていたが、移動する手が1つも無かった。
マウスの左ボタンで引っぱるのは「範囲を囲む」に使っているため。

**iPadだけで確かめていたら見つからない不具合だった。**

**直したこと**

パソコンでは、次の2つで図面を動かせるようにした。

- **ホイールボタンを押したまま動かす**（AutoCADと同じ操作）
- **Shiftキーを押しながら動かす**（ホイールボタンの無いマウス・トラックパッド用）

拡大縮小はホイールを回す（今までどおり）。
左ボタンだけで引っぱるのは「範囲を囲む」のまま。
あわせて、右ボタンなどで範囲を囲み始めないようにした。

案内の文も、機械に合わせて出し分けるようにした（33.3）。
パソコンの人に「2本指で」と書いても分からないため。

**確認したこと（司令塔が実機ブラウザで実測）**

本物の viewport.js と本物の print-ui.js をつないで、マウスの操作を流し込んで測った。

- ホイールボタンを押したまま動かすと、図面が動くこと
- Shiftを押しながらでも、図面が動くこと
- そのとき**四角の図面上の位置のずれが 0**（＝図面に貼り付いている）
- 動かしたあとも、左ボタンで四角を動かせること
- 案内の文が、パソコンむけの文になっていること

実際のアプリでも、ホイールボタンで (-150, -100) 動かしたところ、
**四角もちょうど (-150, -100) 動き、大きさは変わらない**ことを実測した。

テストは219件（新しく6件追加）。3通りのわざと壊す確認を実施。

**まだ確かめられていないこと**：**パソコンでの実際の操作感は、ユーザーに確かめてもらう。**

**返事待ちのこと**：印刷が遅い件（v0.2.3から持ち越し）。
原因は「紙1枚ぶんの絵（写真）」を渡しているためで、アプリ側は0.06秒・186KBしか使っていない。
細かさを200dpiから下げれば軽くなるが、印刷の見た目に関わるのでユーザーの判断を待っている。

### DWGをそのまま開けるようにした（v0.3.0／2026-09-04）

> ユーザー確認：「正常に動作します」（v0.2.6）
> ユーザー指示：「次DWG表示を行ってください」

第1段階で決めていた「DWGは libredwg-web を同梱して読む」を実装した（開発ルール35章）。

**やり方：DWGをDXFに変換して、DXFと同じ道に合流させる**

```
DXFファイル ──────────────────────┐
                                  ├→ dxf-parse.js → 図形データ → 表示・印刷
DWGファイル → [LibreDWG] → DXF ──┘
```

DWGを読み解く係を別に作らなかった。dxf-parse.js には実物の図面で見つけた直しが
たくさん入っており、もう1つ作ると同じ直しを2か所でやることになって必ず食い違うため。

**実物で確かめた結果（参考図.dwg と 参考図.dxf は同じ図面）**

| | AutoCADのDXF | DWGから変換 |
| --- | --- | --- |
| 図形の数 | 602 | **602** |
| 線／円／円弧／楕円／文字 | 482／52／13／2／52 | **同じ** |
| 色ごとの数 | 黒386 赤144 灰41 … | **同じ** |
| 図面の範囲 | -88.41〜5352.33 | **同じ** |
| 表示できなかったもの | HATCH 1・POINT 1 | **同じ** |

**すべて一致。** 数字だけでなく、実際に画面に図面が出ることも目で確認した。

**途中で見つかった不具合（開発ルール34章）**

つないだ直後、**図形が1つも表示されなかった（真っ白）。**
原因は、DWGから変換したDXFでは**全部のレイヤーの色番号がマイナス**で出てくること。
DXFではマイナス＝「そのレイヤーは非表示」なので、全部消えてしまっていた。

同じ図面で比べた実測：

- AutoCADのDXF … 0番=7　DIM=3　PIPE=1
- DWGから変換 … 0番=-7　DIM=-3　PIPE=-1（色の値は同じ。符号だけ違う）

「全部のレイヤーが非表示」はありえないので、そのときは符号を当てにしない、と直した。
一部だけマイナスなら本当に消してあるので、そのまま尊重する。凍結も必ず尊重する。

**10MBの部品をどう配るか（開発ルール35.3）**

DWGを読む部品は約10MB。アプリ本体は全部で0.27MBしかない。

- 事前キャッシュ（アプリ本体）… **0.27MB／25ファイル**（今までどおり軽いまま）
- 部品の枠（DWG用）… **9.86MB／3ファイル**（DWGを開いたときに入る）

部品の枠は**アプリを更新しても消さない**ようにした。消すと更新のたびに10MB読み直しになる。
また、事前キャッシュは「1つでも失敗したら全部やり直し」なので、
10MBを入れると読み込みが1回途切れただけでアプリの更新が止まってしまう。

**確認したこと（司令塔が実機ブラウザで実測）**

- 参考図.dwg（737KB・AC1032＝AutoCAD 2018形式）を開いて、図面が表示されること
- DXFから読んだ結果と、数字がすべて一致すること
- しまい場所が2つに分かれ、アプリ本体0.27MB／部品9.86MB になっていること
- **サーバーを止めた状態（＝電波が無い状態）でページを開き直し、
  覚えていたDWGを開いて、同じ図面が出ること**（描かれた点13577・赤1401、オンラインと同数）
- 「DWGを読む部品を用意しています（初回のみ・約10MB）…」の案内が出ること
- WebAssemblyがgitに1バイトも変わらずに入ること（sha256で確認）

テストは235件（新しく16件追加）。5通りのわざと壊す確認を実施。

**まだ確かめられていないこと**

- **実機（iPad）でDWGを開けるかは、ユーザーに確かめてもらう。**
  とくに、はじめて開くときの10MBの読み込みが、iPadの通信で問題なく終わるか。
- 手元にあるDWGは1つ（AC1032）だけ。古い形式のDWGは試せていない。

**返事待ちのこと**：印刷が遅い件（v0.2.3から持ち越し）。

### 印刷を「絵」から「PDF」に変えた（v0.3.1／2026-09-04）

> ユーザーの指摘：「このプリントはPNG映像で出力印刷していますか。
> もしそうならプリンターが遅いのはそのせいですねえ。一回pdfに落とす事は可能ですか」

**指摘のとおりだった。** v0.2.3から渡していたのは紙1枚ぶんの絵（PNG）で、
2338×1653＝約387万点の集まり。プリンターは紙ぜんたいを点で塗ることになり、
それが遅さの原因だった。v0.2.3から持ち越していた「印刷が遅い」件の答えでもある。

**直したこと（開発ルール36章）**

紙に渡すものを、絵（PNG）から**PDF**に変えた。
PDFは「ここからここまで線を引く」という命令の並びなので、
プリンターは自分の解像度で線を引くだけでよい。

同じ範囲で実測：

| | 中身 | 大きさ |
| --- | --- | --- |
| 絵（PNG） | 約387万点 | 205KB |
| PDF | 線393本・曲線163・文字27・漢字の型2 | **26KB** |

おまけに、**どんな解像度のプリンターでも線がぼやけない**。
それまでは200dpiの点の集まりだったので、600dpiのプリンターでも200dpi相当だった。

**文字の扱い**

- 数字・英字・度記号（`45°`）… PDFに元から入っている書体で書く（線と同じでぼやけない）
- 漢字・かな … 標準の書体に無いので、その文字だけ小さな白黒の型にして貼る

実物の図面では、文字52個のうち漢字を含むのは12個（北 南 東 西 上 下…の方角）だけ。
型は下の線を隠さない形（ImageMask）で貼り、データも縮めてある。

**作っている途中で見つけて直したこと**

1. **狭く囲むと2MBのPDFになった。** 範囲の外にある漢字の型まで作っていたため。
   紙に出ない図形は最初から作らないようにして、14〜88KBに収まるようになった。
2. **確認画面（絵）とPDFで、紙の上の置き方がずれる恐れがあった。**
   置き方の計算を `computePrintPlacement()` 1か所にまとめ、両方が同じものを使うようにした。
3. **上下の取り違えをテストが見逃していた。** わざと上下をひっくり返しても
   テストが通ってしまったので、PDFの中の座標そのものを見るテストを足した
   （これを見逃すと図面がさかさまに印刷される）。

**確認したこと（司令塔が実機ブラウザで実測）**

- PDFがChromeのPDFビューアで正しく開き、図面が出ること（目で確認）
- **漢字（北 南 東 西 上 下 北西…）が、まわりの丸の中にきれいに出ること**（目で確認）
- 同じ範囲の絵（PNG）と見比べて、配置・色・文字が一致すること
- アプリの「プリント」を押したときに渡されるのが
  `参考図_20260904_1333.pdf`／`application/pdf`／26KB であること
- その中身が、線393本・曲線163・文字27・漢字の型2・A4横 であること

テストは259件（新しく24件追加）。6通りのわざと壊す確認を実施。

**まだ確かめられていないこと**

- **実機（iPad）で、PDFがちゃんと印刷できるか。**
  とくに、共有メニューに「プリント」が出るか、そして**本当に速くなったか。**
- 手元のプリンターで試せないため、速さの改善は数字（205KB→26KB、387万点→約550本）
  からの見込みであって、実測ではない。

### パソコンの印刷を直した／「PDFで保存」を足した（v0.3.2／2026-09-04）

> ユーザー報告：「iPadの方ではすべて正常です。**プリントも通常通りの速さに戻りました。**
> それからpcのほう、これは共有メニューにプリント項目はありません。
> 以前のような形式のほうがいいんですけど」
> ユーザー要望：「これならpdf出力にも対応できるのではないですか」「pdfで保存という機能追加して下さい」

**v0.3.1のPDF化で、iPadの印刷の遅さは解決した**（v0.2.3から持ち越していた件が片付いた）。

**(1) パソコンで印刷できなかった（開発ルール37章）**

原因は見分け方の間違い。それまでは

```js
if (blob && typeof navigator.share === 'function') { /* 共有メニューへ */ }
```

と「**共有できるか**」で判断していた。だが知りたかったのは
「**共有メニューにプリントがあるか**」で、これは同じではない。

Windowsのブラウザにも共有メニューはあるが、中身はメールや近くの人への送信だけで、
**「プリント」が無い。** 渡しても印刷できなかった。

直したこと：

| 機械 | 印刷のやり方 |
| --- | --- |
| iPad・iPhone | 共有メニューへ渡す（今までどおり。正常に動いている） |
| パソコン | **ブラウザの印刷画面を直接開く**（以前のように、押したらすぐ出る） |

パソコンでは、見えない入れ物にPDFを読み込ませて、その中で印刷する。
ページを印刷するのではないので**URLも日付も入らない**し、
渡すのはiPadと同じPDFなので**速さも同じ**。
印刷画面を開けなかったときは、PDFを新しいタブに出す（何も起きないのを避ける）。

機械の見分けは `src/ui/device.js` の1か所にまとめた。
iPadOSはUAが「Macintosh」と名乗るので、指で触れる点の数と合わせて見分けている。

**(2) 「PDFで保存」を足した（開発ルール37.4）**

PDFはもう作ってあるので、保存はそれを渡すだけ。確認画面にボタンを足した。
iPadでは「ファイル」アプリへ、パソコンではダウンロードのフォルダーへ入る。
PDFが作れず絵になったときは、ボタンの名前が「画像で保存」に変わる（うそをつかない）。

**確認したこと（司令塔が実機ブラウザで実測）**

- **共有メニューが使える状態にしても、パソコンでは共有メニューを使わないこと**
  （わざと `navigator.share` を用意して確かめた）
- そのとき、PDFを読み込んだ見えない入れ物（1px×1px）ができること
- 機械の見分けが正しいこと
  （このPC=false／iPadOSのふり（Macintosh＋タッチ5点）=true／UAにiPad=true）
- 「PDFで保存」で `参考図_20260904_1410.pdf` が保存されること
- ボタン3つが横に並んで、どれも高さ48px（指で押せる大きさ）で、はみ出さないこと

テストは266件（新しく7件追加）。7通りのわざと壊す確認を実施。

**まだ確かめられていないこと**

- **実機（パソコン）で、押したら印刷画面が出るか。**
  この環境では印刷画面そのものを開くと確認作業が止まってしまうため、
  「そこまでの道が正しいこと」までしか確かめていない。
- **iPadで「PDFで保存」がファイルアプリに保存されるか。**

### ハッチング（斜線）と点に対応した（v0.3.3／2026-09-04）

> ユーザー：「すべて正常です」（v0.3.2）
> ユーザー：「私は席を離れるので、クロードの考えで進めれるところをできるだけ進めて下さい」

実物の図面（参考図）で、こう出ていた残りを片付けた。

> この図面のうち 2個の図形は表示できませんでした（種類：HATCH 1個、POINT 1個）

**この案内が 0個になった。**

**(1) ハッチング（開発ルール38章）**

「この範囲を斜線で埋める」という指定。断面や壁を表すのに使う。

新しい図形の種類は増やさず、**ふつうの直線の並びに直す**ことにした（`src/hatch.js`）。
種類を増やすと、画面・PDF・範囲の計算・切り取り判定…と直す場所が一気に増えるため。

- 囲いを点の並びにする（円弧・楕円弧・ふくらみも直線に直す）
- 模様の決まりから平行線を並べ、**囲いの中に入っている部分だけ**を切り出す
- 切り出しは「辺を何回またいだか」で決めるので、**穴のあいた形（ドーナツ）も正しく抜ける**

実物の図面で確かめて分かったこと：

- 模様の角度・間隔は、**すでに拡大率と回転を掛けたあとの値**だった
  （コード52が630度・41が24なのに、模様の角度は225度）。重ねて掛けてはいけない
- 模様の基準の点が、図形から**1万以上離れていた**。
  基準から順に数えると囲いにたどり着かないので、囲いの位置から何本目かを先に計算する

べた塗りは、塗りつぶす仕組みが無いので囲いの形だけ描く（SOLIDと同じ扱い）。
囲いの無いハッチング（ほかの図形にぶら下がっているだけのもの。実物にあった）は、
描くものが無いので何も出さず、数えもしない。

**(2) 点（POINT）**

CADの設定 `$PDMODE` が 0（＝点を小さな丸で表示する）だったので、同じように小さな丸を出す。
大きさは線の太さに合わせるので、画面でも紙でもちょうどよい大きさになる。
寸法の中の点は、今までどおり出さない（実物で100個近くあった）。

**確認したこと**

- 実物の参考図：**表示できなかったもの 2個 → 0個**、図形602個 → 603個（点が1つ増えた）
- 見本の図面（100×100の四角を45度・間隔10で埋める）で、
  斜線が**15本・すべて45度・四角からのはみ出し0・間隔ちょうど10**
- べた塗りの見本で、閉じた四角の囲いが1つ出ること
- ブラウザの本物のCanvasで、斜線が描かれること（黒い点11066個が正方形の範囲に広がる）
- ブラウザの本物のCanvasで、点が**図面の(30,40)に当たる画面の場所**に丸として塗られること

テストは293件（新しく27件追加）。5通りのわざと壊す確認を実施。

**分かったこと（記録）**

参考図のハッチング2つのうち、模様つきの1つは**ブロックの中**にあり、
そのブロックは図面に配置されていなかった。
そのため実物の図面では、この対応で見た目は変わらない。
見本の図面を作って確かめた。

**残っている機能**

- 2点をタップして寸法を測る
- 図面に文字を書き足す

## 何を作るアプリか

管工事の実務で使う、**2D CADビューア＋範囲指定プリント**のWebアプリです。
現場や出先で、iPadからCADの図面（DXF／DWG）を開いて確認し、
**必要な部分だけを枠で囲んで印刷する**ことを一番の目的にしています。

サーバーは使いません。読み込みも解析も描画も、すべて端末のブラウザの中だけで終わります。
図面ファイルがどこかへ送られることはありません。

---

## 決まったこと（記録）

### 1. 対応する形式：DXF と DWG の両方（2026-09-02 ユーザー決定）

- DXFは自前のJavaScriptで読む。
- DWGは `libredwg-web`（LibreDWGをWebAssembly化したもの）を**ファイルとして同梱**して読む。

**この決定に伴う重要な条件（ユーザー了承済み）**

`libredwg-web` の使用許諾は **GPL-3.0** です。これを組み込んだアプリを配布すると、
**アプリ全体をGPL-3.0にして、中身（ソースコード）を誰でも見られる状態で公開する**義務が
発生する、というのが一般的な解釈です。

- したがって、このアプリの置き場所（GitHubリポジトリ）は**公開（public）**にします。
- フォルダーに `LICENSE`（GPL-3.0の全文）を置きます。
- 図面ファイルそのものは公開されません。公開されるのは**アプリのプログラムだけ**です。
  お客様の図面はiPadの中だけで処理され、どこにも送られません。

※ここは法律の判断が絡む部分です。会社として気になる場合は、着手後でも方針を変えられます
（DWG対応を外せばGPLの制約はなくなります）。差し込み口を分けて作るので、後から外せます。

### 2. 必要な機能（貼り付けられた仕様書に準拠）

1. **ファイル読み込み** … ファイルアプリ／iCloud から `.dxf` `.dwg` を選んで開く
2. **ビューア操作** … スワイプで移動、ピンチで拡大縮小
3. **簡易マークアップ** … 2点タップで寸法測定（mm）、任意の場所へ文字を追記
4. **範囲指定プリント（最重要）** … 半透明の枠をドラッグで置き、その範囲だけを印刷する

※レイヤーの表示切替は仕様書に入っていないため、v1では作りません。
　図面が混み合って見づらいときに効く機能なので、実際に使ってみて必要そうなら後で提案します。

### 3. 使う端末：iPad中心。SafariでもChromeでも動かす（2026-09-02 ユーザー決定）

**調べて分かった大事なこと：iPadのChromeでは、オフラインのアプリにできません。**

iPadのブラウザは、Chromeも中身はSafariと同じ仕組みで動く決まりになっています。
そのため「ホーム画面に追加してアプリのように使う（＝オフラインで開ける）」は、
**Safariでしか使えません**（EUなど一部地域を除く）。

そこで、こう作ります。

- **Chromeでも普通に見られる**ようにする（毎回ブラウザでURLを開く形。電波が要る）
- **オフラインで使いたい人には、アプリの中で「Safariで開いてホーム画面に追加」を案内する**
- ホーム画面に追加すれば、電波の無い現場でも開ける

※日本でも規制の動きがあり、状況が変わっている可能性があります。
　**実際のiPadで必ず確かめます**（開発ルール7.3「実物を自分の目で見る」）。

### 4. 作り方（技術構成）：ユーザーから司令塔へ一任。下記のとおり決定

- **素のJavaScript（ES2022）＋ESモジュール。ビルドは使わない。**
  貼り付けられた仕様書には Vite + React + TypeScript とありましたが、採用しませんでした。
  理由：`開発ルール.md` 1章のとおり、数年後にビルドが通らなくなる事故を避けるためです。
  今回の機能（範囲指定プリント・寸法測定・図面の描画）は、どれもCanvasと素のJavaScriptで
  書けるもので、フレームワークを入れて楽になる部分がほとんどありません。

- **外部の部品は、npmやCDNではなく「ファイルとしてフォルダーに置いて」使う。**
  `vendor/` に実ファイルを置きます。ビルドは不要のままです。
  （共通ルール「考え方差分記録」の、外部サーバー頼みを避ける方針に合わせています）

- **描画は HTML5 Canvas の2D。Three.js は使いません。**
  扱うのは2Dの図面なので3Dの仕組みは不要で、
  さらに「見えている範囲を画像にして印刷する」処理がCanvasだと素直に書けるためです。

### 5. 内部の作り（一番大事な設計）

**DXFもDWGも、いったん同じ形の「図形データ」に変換してから描きます。**
描く側は、元がDXFだったかDWGだったかを知りません。

```
DXFファイル ─→ dxf-parse.js ─┐
                              ├─→ 共通の図形データ ─→ render.js（描く）
DWGファイル ─→ dwg-parse.js ─┘
```

こうしておく利点：

- DWG対応（GPLの部品）を、後から丸ごと外せる
- 描画・印刷・寸法測定は、形式の違いをまったく気にしなくてよい
- 片方が壊れても、もう片方は動き続ける

---

## これから作る順番

| 段階 | 内容 | 完了の目安 |
| :--- | :--- | :--- |
| 第1段階 | DXFを読んで画面に描く。スワイプ・ピンチで見る | 実際の図面がiPadで見られる |
| 第2段階 | **範囲指定プリント**（最重要） | 囲った範囲だけが紙に出る |
| 第3段階 | 寸法測定・文字の追記 | 現場で寸法が測れる |
| 第4段階 | DWG対応（同梱部品を組み込む） | DWGを直接開ける |
| 第5段階 | PWA化・公開 | iPadのホーム画面から起動できる |

各段階の終わりに、必ず実物をiPadで確認します。

---

## まだ決まっていないこと

- 印刷の縮尺の扱い（原寸で出すか、用紙に合わせるか）は範囲指定プリントを作るときに詰めます。
- レイヤーの表示切替を入れるかどうか（実際に使ってみてから判断）。
- PWA化（`service-worker.js` と `manifest.json`）はまだ入れていません。
  今はブラウザでURLを開く形。オフライン対応はこのあと入れます。

## 参考にする既存アプリ

`H:\マイドライブ\AIアプリ制作\04寸法取りアプリ制作`
配管の寸法取りアプリ。同じ作り方・同じ運用でここまで来ています。
特に `service-worker.js` は、iPadでアプリが開かなくなる本番事故の対策が
コメント付きで入っているので、そのまま流用します。
